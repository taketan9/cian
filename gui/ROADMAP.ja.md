# cian-gui（Electron 版）ロードマップ

`cian-core` の上に、Chromium で描くもう1つのフロントエンド。端末版（`cian-tui`）は
生き続け、winit + wgpu の `cian-gui` は凍結する。

**この文書は「これから」だけを持つ。** 2026-08-31 に 1295 行・見出し90個まで積み上がり、
新しい「いま」を上に足し続けた結果、同じ文書が2つ溶接された形になっていた。直した
ものの一覧は `git log` にある（この repo のコミットメッセージは長い）。ここに残すのは
**次に手を動かす人の判断を変えるもの**だけ。

---

## この計画が存在する理由

**見た目ひとつ。** 速度は 2026-08-24 に解けた（`want_cpu_renderer` の自動判定）。
残っていたのは「会社の Windows で文字が残念」で、原因は cian の**どちらの描画経路も
OS の文字描画を使っていない**ことだった。Chromium は Windows で DirectWrite を使う。

**2026-08-25、実機で判定が出た。** 3つ並べて、Electron 版は `cian.exe`（「昭和の
コンピュータっぽい」）より明らかに読みやすく、日本語も潰れない。以後の投資はこの1点の
実測に立っている。

## 起動のしかた

```
gui/run.bat        Windows
gui/run.sh         Mac / Linux
```

Electron の場所は探しにいく（`$CIAN_ELECTRON` → `gui/electron.txt` の1行 → リポジトリの
隣の配布版 → `node_modules`）。**見つからないときは、どこを探したかを言って落ちる。**
エンジンが無いときも同じで、空の窓を見せない。`gui/engine.js` は 隣 → target/release →
target/debug の順で探し、**新しい方が勝つ**。

**エンジンを触ったら `cargo build -p cian-server`。** `cargo test` は bin を更新しないので、
古いエンジン相手に「直っていない」と読むことになる。

## 設定 ── 窓が何を読むか

`init.lua` は端末版と**共用**（`%USERPROFILE%\.config\cian\init.lua`、
`CIAN_CONFIG_DIR` で置き場所を変えられる）。ただし読む項目は端末版と同じではない。
エンジンの `settings` が返すもの（`crates/cian-server/src/main.rs`）:

- **`cian.set_option` から**: `home` `editor` `shell` `view` `key_hints` `edit_style`
  `show_hidden` `tab_width` `lang` `notify` `notify_min_secs` `preview` `transfer_limit`
- **あるかないかだけ**（メニューの行を出すかの判定に使う）: `cian.ai` /
  `cian.snippets` / `cian.ssh` / `macro.lua`。端末版の `menu.rs` も同じ3つを訊いて、
  答えが no の行を出さない
- **`cian.set_keymap` の全部**（端末版と同じ表を読む ── 片方で効いて片方で効かない
  束縛は、1つの名前を着た2つのプログラム）
- **Lua が拒んだもの**（`config_errors`）も返して画面に出す。出さないと、init.lua の
  打ち間違いが「黙って無いだけの設定」になり、人は違う所を探しに行く

**窓が自分で決めたことは `state.toml`**（端末版の状態ファイルそのもの）に入る ──
`theme` の隣に `gui_look` `gui_editor` `gui_font` `gui_view` `gui_hints`
`gui_main_pct` `gui_panes_pct`。**`theme` は共用なので、窓で選んだ配色で端末版が開く。**
`gui_font` を `font_level` と分けてあるのは、あちらが端末エミュレータの
ポイント数で、こちらが窓のピクセル数だから（1つの名前に2つの意味を持たせない）。

**`init.lua` は書き換えない。** あれは手で書かれ、コードとして読まれるファイルで、
書き換えるプログラムは人のコメントを整形し直すことになる。

---

## 目指す先の大きさ（端末版の実測）

| | |
|---|---|
| `cian-core` | 12,100行 — **そのまま使う**。UI を1行も含まない |
| `cian-tui` | 59,289行（うちテスト 17,543）— **持っていかない**。仕様書として読む |
| `:` コマンド | 110腕 / 179名前 |
| メニュー項目 | 185 |

**移植ではなく再決定。** キーマップ、コピー／移動の作法、diff ピッカー、vim 文法は
「同じ結論にもう一度たどり着く」作業になる。ここが一番時間を食う。

---

## いまの到達点

M0〜M5（背骨・ファイラ・検索・エディタ・シェル・SFTP）は通っていて、そのあと
**端末版を正とする突き合わせ**を次元ごとに5周した:

1. **コマンド名の辞書差ゼロ**（2026-08-30）
2. **挙動と見た目**（同日その3）── キーの食われ9件、転送の無断上書き、フォント同梱、
   一覧の列構成、ステータスバーの常設チップ列
3. **深さ**（同その4）── 進捗モーダルと本物のキュー、`show()` の絞り込み、IME 全面追従
4. **マウス面・ポップアップ56種・メニュー110種の全数調査**（2026-08-31）
5. **表記と挙動の揺れ**（同日、commit `6a910f3`）── 同じものを二つの前端が別の言葉で
   呼んでいた 30 箇所。ヘルプ 159 行も端末版 manual 183 項目と突き合わせた

**Windows 実機は 2026-08-31 に初回2周。** 通ったもの: Ctrl+A / Space マーク /
Ctrl+Shift+矢印 / F11 / Ctrl+E / 配色 / シェル最下行 / run.bat。

**2026-09-01、v1.1.1 の資材で2回目。16 件出て、13 件を直した**（`4c4aa01`）──
凍った `tr()`／メニューのスクロールとマウス／`inset box-shadow` とスクロールバー／
`o` `O` のヘルプが逆／`:ssh` がリモートペインを開いていた（端末版はシェルで走らせる）／
`:sftp` が init.lua を見ていない／cmd.exe → PowerShell／zip リネームが出ない／
ディレクトリのアイコン／エディタのタブ列／終了時の write EOF。
**残り3件は再現できていない** ── `.md` が開かず入力も効かなくなる、F10 で2タブ、
初回だけ IME オフ。次の資材で**画面に出た文字**を貰う。

**2026-09-01、v1.1.2 の資材で3回目。4件出て、全部直した**（`bf74329`）──
シェルタブの巻き添え（一巡目からの持ち越し。`shellpaneclose` が、誰のものでもない
id を「いま見ているタブ」に落としていた）／`list` が `Pane::new` でペインを作り直し、
`o` `O` `z` `:cd` と **F5** が履歴・マーク・並び順・隠し設定を毎回捨てていた／
メニューのマウス問題の2つ目の扉（`scrollIntoView`）／シェルパネル上辺の平行線3本。

**検証中に別途2件**（`7b14508` / `0beb03f`）── アイコンを案B「カーソル行」に
（旧アイコンは速度線つき矢印が主語で、16px では矢印しか残らなかった。焼くのは
`packaging/icon.py`、ライブラリ無し）。**窓には `icon:` が無く、タスクバーは
Electron の既定を描いていた。** 題は README の頭字語から
`cian - Comfortable Interface for Agile File explorer Navigation`（x は小文字、彼の指定）。

**2026-09-01、v1.1.4 の資材で4回目。3件出て、全部直した**（`d083c16`）──
シェルが固まって見えるのは**スクロールバックから戻れない**だけだった
（`vt100` は文字をスクロールバック側から、カーソルを生きている側から返す。
`write_input` が `scroll_to_bottom` を呼んでいなかった）／`Alt+` ` を ESC+` に
変えて食べていた（IME のキーは IME に返す）／シェルパネルの枠が上辺の一本だけ
だった（ファイルペインは四辺）。あわせて **Windows でタスクバーのボタンが2つ**
になる件 ── `setAppUserModelId` 未設定と、`run.bat` が Electron を前面で
走らせてコンソールを残していたこと。

**2026-09-01、v1.1.5 の資材で5回目。5件出て、全部直した**（`8b17d78` / `2fb0c69`）──
`/` `:` `v` シェルの色を**キーのあるペインの枠**へ（端末版 `focus_badge_color`。
窓版は画面最下部だけ塗っていた）／`s` のパス列が揃わない・毎回最上位から開かない／
エディタの `?` が宣伝されていて未割当（vim の後方検索に食われていた）／
文字コードはあったがヒント行がメニューを名指ししていなかった／
追随プレビューが全画面（端末版はシェルパネルの画素を借りる）／`=` が同一のとき
下端で囁くだけ。そして **System32 のもっさりは 1行 = 1 DOM ノード**で、
カーソル移動のたびに 5000 個作って捨てていた ── **645ms → 7ms**（見える行だけ描く。
300件未満と icons 表示は従来どおり）。

**2026-09-01、v1.1.7 の資材で6回目。6件。うち2件は「直したのに効かなかった」**
（`?` と `Alt+``）── 原因は同じで、**`e.key` で待っていて `e.code` で待って
いなかった**。日本語キーボードの `半角/全角` は US のバッククォートの位置にあり、
`key` は `Zenkaku` / `Process` / `Unidentified` のどれにもなる。**Mac で通っても
JIS 配列では外れる**の一例。ほかに `q` が未割当（端末版は確認つきで終了）／
検索の結果シートに緑枠／`h` の「いまここ」→「現在地」／`=` を Esc 一回で閉じる。

**ビルドは Windows の窓版だけになった**（2026-09-01「しばらくは Windows の gui
モードでしか動作させない」）。タグ push → `cian-gui-win-x64.zip` とエンジンだけ、
11分 → 数分。他は消していない ── `release` を手動実行して
**everything = true** で従来の7点。

**v1.1.8 はフォント（`vendor/fonts/cian.ttf`、6MB）が入っていない** ── gui-only 化の
ときに font fetch まで `everything` で閉じてしまった。`vendor.js` は見つからなくても
行を出して続けるので、**黙って 6MB 軽い zip が出た**。staging に「無ければ落ちる」を
足し、v1.1.8 のリリースは消した（タグは残す）。**検査が黙る**の型がまた出た。

**アイコンは案E「かぎ括弧」に差し替えた**（2026-09-01、彼の希望で B から）。
日本語の「 」が左右の枠になり、その間に一行 ── 並のファイラーの棚で確実に浮く。
**16/24/32/48 では真ん中の一行を描かない** ── 提案時は「16px で消える」と
書いたが、焼いてみると 32 と 48 では消えるのではなく**」に寄って汚した**。

バージョンは **1.1.10**（2026-09-01、この日10回目。
勝手には上げない ── [[feedback-versions-are-taketans-call]]）。

---

## 毎回通す検査

**画面を見ずに直すと必ず失敗する。** Taketan の 2026-08-30 の言葉:「全般的に、全然
完成度が低い」── 原因は画面を見ずに直していたこと。

1. **`node gui/drive.js`** ── CDP で本物の窓に本物のキーを送る。**基準は例外 0 件。**
   先に `pkill -f "Electron.app/Contents/MacOS/Electron"; sleep 1`（前のプロセスが
   ポートに残ると `the window never appeared` や無関係な ReferenceError が出る）。
   `list` で開いているシートの行を全部読み上げ、`shot:名前` で
   `$TMPDIR/cian-shots/名前.png` に撮る。砂場に自分の config を切るので実設定は汚れない
2. **拡大スクリーンショット**（`clip: {…, scale:3}`）── CSS の後勝ち、`hidden` な見出しが
   空白帯として場所を取る、シェルが panel の 1/3 しか使っていない、ビューアのヒント行が
   一覧のキーのまま ── どれもこれでしか見つからなかった
3. **`python3 scripts/audit.py`** ── 「きれいです」が基準
4. **`cargo test -q --workspace` / `cargo clippy -q --workspace --all-targets -- -D warnings`**
5. **`python3 scripts/requests.py`** ── `gui/REQUESTS.ja.md` の21行。**lint ではなく契約。
   落ちたら直す前に訊く**
6. **`python3 scripts/keycover.py`** ── ヘルプが名前を挙げるキーのうち drive.js が一度でも
   押した割合（14/72）。壊れていた `Ctrl+E` と vim 流の `Ctrl+C/X/V` はどちらも
   「一度も押していない」側にいた
7. **`python3 scripts/parity.py`** ── 端末版と窓版が同じものを同じ言葉で呼んでいるか

CI は Rust 3-OS ＋ front-end ジョブ（`node --check gui/*.js` ＋ 3〜7 のうち Rust 以外）。
**drive.js は CI に無い**（画面と Electron が要る）ので押す前に手で回す。

**検査に checks を足したら必ず変異テストする。** 実際に5回、「指摘が消えた」ではなく
「検査が黙った」をやっている。壊れた版を仕込んで指摘が出ることを確かめ、戻す。

---

## 繰り返し出たバグの型（足す前にこれを疑う）

1. **書いたのに届かない。** 実装があり、ヘルプにも載り、どこからも実行できない。
   **修飾キー付きは修飾なしより前に置く**（if/else の鎖は上から効く）。素の1文字キーには
   `bare`（!ctrl && !meta）ガード。足したら drive.js で**そのキーを押す**
2. **監査が黙る。** 指摘が消えたのか検査が死んだのか、出力は同じ → **変異テスト**
3. **CSS の後勝ち・UA 負け・親の重なり文脈。** `hidden` はブラウザ既定の `display:none`
   なので `.row{display:flex}` に負ける。**親に z-index が無いと子は親の層に閉じ込められる**
   → 拡大スクショ。`getBoundingClientRect` で矩形を測り、`elementFromPoint` で
   「本当に最前面か」を訊く
4. **測る対象を間違える。** `measureCell()` がシェルの**外**の行間を測って、12行入る場所に
   「7行」と PTY に教えた → **実物と同じ条件で測る**（同じ class のプローブ）。
   時機を推測せず `ResizeObserver` で箱の変化そのものを見る
5. **道具の報告が観測を隠す。** drive.js の LOOK がシェル行をビューア行より優先していて、
   動いている F3 が「効かない」と読めた → **「効かない」の前に道具の報告経路と相手の
   ビルドを疑う**
6. **移植の物差しを1つしか当てていない。** 「辞書差ゼロ」で済んだ気になっていた。
   物差しは `mouse.rs` の当たり判定登録・`Popup` enum・`MenuItem` enum・`ToggleId`・
   **`MenuItem::label` の日本語** → **次元ごとに機械で数える**
7. **画面に出たことがないコードは、間違っていても誰も気づかない。** `hintsNow()` に
   ビューアの枝が丸ごとあったのに `drawHints()` は `say()` からしか呼ばれず一度も
   描かれず、しかもその枝は `style === 1`（vim）でメモ帳の行を返していた
   → **状態を持つ変数は1つの扉にする**（`setViewerOn()`）。足したら `shot:` で撮る

---

## 残っている差

### 機能が無いもの（`scripts/parity.py` の KNOWN と1対1）

**2026-09-01 に9つとも入った。`scripts/parity.py` の免除は 0 件。** 背景色
（14色・ライブ・ペイン別）／テーマ（このペイン）／プログラムから開く／情報を見る／
Office で開く・ショートカット／このファイルを要約（`:summary`）／mermaid（`:mermaid`）／
転送 ▸／このペインを同時入力に含める・外す／**言語**。
OS 連携は `cian_core::os`、ペイン背景色は `cian_core::theme`、mermaid は
`cian_core::mermaid` へ移して**両前端で1つの実装**にした。

**言語**は `tr(en, ja)` ── 端末版と同じ引数の順にしてある（行を移すときに裏返さずに
済み、日本語がソースに残るので parity.py が読める）。覚えない（端末版もそう。
言語は init.lua のもので、窓だけが別を覚えたら同じ機械で食い違う）。
**`python3 scripts/i18n.py` が残りを数える** ── 1301 / 1301。訳さないと決めた5件
（配色の名前3つと、切替のラベル自身2つ）は理由つきで除いてある。

### マウスで届かないもの

**残りはサイドバー（場所の一覧）だけで、これは窓に概念が無い。**
ファイル行のドラッグ（反対ペイン／シェル／デスクトップ）・シェル分割の内部境界・
ビューアのタブ列・パンくずの `◀ ▶` は全部入っている。

**2026-09-02 に、この節が丸ごと古いことが分かった。** 「マウス3つ・小物3つが
未実装」と書いてあり、その通りに作りにいったら6つとも既にあった。**書いた本人が
自分の書いたものに騙された。**下の一覧は、書いた時点で1つずつ実物に当たっている。

### ポップアップ

| 端末版 | 窓版 | |
|---|---|---|
| `ConfirmZipAdd` | **2026-09-02 に入れた** | それまでは zip の**隣**に落として「コピーしました」と言っていた |
| `ConfirmElevate` | **2026-09-02 に入れた** | Windows が拒否したら管理者でやり直すか訊く。それまでは諦めていた |
| `ConfirmZipDelete` | 入っている | |
| `LocalDest`・`ConfirmRemoteMove` | 作らない | 「いらない」（2026-09-10、本人）。窓版のリモートはペインなので、行き先は反対のペイン |
| `AiChat`・`AiHistory` | 作らない | 「会話はいらない」（2026-09-02、本人） |

小物3つ（`Search` の開いたまま↑↓・`GrepReplace` の `f`・`CommitMessage` の `e`）は
**3つとも入っていた**（2026-09-02 に確認）。

### 挙動の深さ

### リモート ── 2026-09-02 に**実際に通した**

**手元にサーバが無い、がずっとの障害だった。無かったのは発想の方。**
`sshd` と `sftp-server` は macOS にも Linux にも最初から入っていて、
**管理者権限なしで** 127.0.0.1 の空きポートに自分専用のものを立てられる。
`python3 scripts/remote.py` がそれを毎回立てて、通して、片付ける。

立てた日に出たもの:

- **ローカル→リモートの `c` が、手元にコピーして「1件コピーしました」と
  言っていた。** リモートペインの `cwd` は接続前のディレクトリのままなので、
  行き先が手元の適当な場所になる。**サーバは一度も触られない。**
  zip の件と同じ型で、今日これで4件目
- **鍵で入れなかった。** `Target` はパスワードだけを持っていた ──
  なのに `examples/ssh.lua` は前から「鍵認証を使ってください」と書いていた。
  **無い機能を勧めていた**
- 入れたもの: ローカル↔サーバの `c`/`m`（進捗は同じバー）・サーバ内の移動・
  サーバ間の中継（一時ファイルは必ず消す）・**同一サーバの移動は rename**
  （中継すると全バイトが往復し、rename の原子性も失う）

`:chmod` は前からリモートに届いていた（今日初めて確かめた）。

**2026-09-02 の続きで、リモートは仕上げた。** `scripts/remote.py` が本物の
サーバ相手に **21 項目**を通す:

- 鍵で入る／ローカル↔サーバ／サーバ↔サーバ（中継・同一ホストは rename）
- **ディレクトリごと**（`plan_upload`/`plan_download` が木を歩き、親から順に
  mkdir する。**これは端末版にも無かった** ── 両方ファイルだけ送っていた）
- サーバ内の移動・chmod・作成・改名・**中身ごと削除**
- リモートのファイルを開いて書き戻す
- **転送レートの上限が実際に効く**（400KB を 200k/s で 2.03秒、上限なしで 0.08秒）
- AI の走査はリモートペインで断る

**保存先ピッカー（`LocalDest`）は窓版には作らない。** 端末版がそれを持つのは
リモートが*モーダルなブラウザ*だからで、窓版のリモートは*ペイン*。行き先は
反対のペインで、`z` でどこへでも動かせる。同じ機能を窓の設計に移すと、既に
ある道の上にもう一本の道を敷くことになる。**2026-09-10 に本人から「いらない」。**

| | 残り | 備考 |
|---|---|---|
| 低 | ディレクトリの転送に確認シートが無い | いまは件数だけ。中身の一覧を見せてもよい |
| 低 | 中継の再開（途中で切れたら最初から） | |
| 中 | ビューア: notepad 流でも `:` 面へ届く道・対ディスク差分ガター | 端末版 `viewer.rs:1526` |
| — | **2026-09-02 に片づいた**: tar への書き戻し（`tar_modify`）・スクリプトマクロ（窓版も走る）・`macro/` ディレクトリ（窓版は `macro.lua` しか読んでいなかった）・ディレクトリ転送の確認シート | |
| 低 | 中継の再開（途中で切れたら最初から） | |

md プレビューの相対リンクと `#見出し` は 2026-09-02 に入れた（`markdown::slug`）。
アイコンビューの本物のファイルアイコンも入っている。

### まだ突き合わせていない次元

無し。確認シート・レポート本文・エラーメッセージも 2026-09-01 に両言語化の
過程で全部読み直した。

---

## 意図して残した逸脱（バグではない）

- **`:render` という名前** ── 端末版はビューア内 `:preview`。GUI は追従プレビューと
  衝突するため別名（vim 流儀の `:preview` は端末版どおり）
- **`:branch` `:local` `:tab` などの命名** ── キーにしか無い機能にも、パレットに出す
  名前が要る
- **一覧のホイールはビューだけ動かす**（端末版はカーソルが動く）── 窓の作法としては
  現状が正しいと判断。**要相談**
- **窓版にしかないもの** ── メニューの `新規タブで開く`・`動いている処理`・`全画面`・
  `この面を広げる`、スイッチの `キーヒント`・`一覧の見せ方`、コマンドの `:forward`、
  ビューアの `Markdown プレビュー`・`見出しから飛ぶ`
- **mermaid** ── 端末版はブラウザで開く（`:mermaid`）。窓はプレビューの中に描く（`Ctrl+E`）

---

## 検証できていないこと（正直に）

**「僕が通していない」と「動かない」は別のことです。** ここに並ぶのは前者です。

- **AI の実応答と SFTP は Taketan が前の版で通している**（2026-09-02 に本人が確認）。
  僕の手元では今も再現できない ── endpoint が社内、SFTP サーバが無い ── ので
  ここに残しますが、**未実装でも未達でもありません**
- Windows 未確認: **Ctrl+クリック**（Mac は Cmd 側の枝しか通らない）、**IME**、
  **vim 流の貼り付けそのもの**
- **コピーの取り消しが実際にゴミ箱へ入るところ**（2026-09-02 に入れた）。この Mac は
  Finder への Apple Events が拒否されていて、`trash` クレートは macOS では
  AppleScript 経由なので `-1743` で止まる。**キーがエンジンまで届き、正しい1本を
  選び、失敗を読める形で言い、手順を積み直して再試行できる**ところまでは通した。
  Windows の `trash` は Shell API なので権限の話は無い

### AI ── 会話窓ではなく、質を上げる方向で（2026-09-02）

**「会話はいらない。その代わり提案・検索・ゴミ検索の精度を上げたい」**（本人）。
入れたのは、モデルの判断力ではなく**渡している事実**です。

- `cian-core/src/survey.rs` ── 木を歩いて、**種類・サイズ・最終更新からの日数・
  パス**の4列を作る。判断は一切しない（ゴミの語彙はプロンプト側にある）。
  **ディレクトリのサイズは配下の合計** ── これが空欄だったので、ゴミ検索は
  `node_modules` を「名前の響き」で判断していた
- ゴミ検索は**4階層下まで**（前は1階層）。入れ子は外側だけを挙げさせる
- 意味検索は隠しファイルも見る（`.eslintrc` を探す人がいる）。ディレクトリも
  候補に入れる（「認証はどこ」の答えはフォルダのことがある）
- コマンド提案は**OS ではなくシェル名**を渡す。Windows で PowerShell と cmd を
  取り違えるのが一番よくある外し方だった。いま開いている一覧とマークも渡す。
  1行で書けないときは `# ` で断らせる（もっともらしい半分の答えが一番困る）
- **打ち切ったら言う。** モデルには英語で、人には母語で。「N 件入らなかった」
  ではなく「何階層目までは全部見た」 ── 前者は Rust の checkout で
  「42160 件入りませんでした」になり、正しくて役に立たなかった
- プロンプトは `cian-core/src/aiprompt.rs` に1本ずつ。**両前端に写しがあった**
  （6本）。`scripts/parity.py` が、同じ文章が2つのクレートに現れたら落とす

**そのシェルが「いまどこにいるか」**（2026-09-02、本人の質問から）:

- **端末版はずっとやっていて、窓版は何もしていませんでした。** タイトルの
  `user@host`／画面に残る `ssh …` 行（AIX の ksh はタイトルを出さない）／
  プロンプトの形（`PS C:\` か `$`/`#` か）の3つを見て、`init.lua` の
  `ssh_hosts[].notes` に書いてあれば**接続先のOS**まで分かる。窓版の `:aicmd`
  は Linux サーバに向かって PowerShell を書いていた ── しかも答えは**その
  プロンプトに貼られる**ので、唯一実行できない場所に置かれることになる
- `cian-core/src/shellwhere.rs` に移して両方から。プロンプトも1本
- **`cian.set_option("shell", …)` を窓版が無視していた。**端末版だけが効いて
  いた ── 効かない設定は、無い設定より悪い（外からは区別が付かない）
- **リモートペインでは `:aijunk` / `:aistructure` / `:aisearch` を断る。**
  リモートペインの `cwd` は接続前のローカルパスなので、**別のマシンを歩いて
  「これがリモートの中身です」と答えていた** ── どの行ももっともらしいので、
  一番たちの悪い間違い方

**通していないもの:** 実際のモデルの応答。手元から社内 endpoint に届かない。
プロンプトの組み立てと「どこにいるか」の判定は cian-core のテストが通している。

### 追いかけている不具合

- **`getLineChanges()` が3箇所の相違を1箇所と答える。** `d1.txt` と `d2.txt` は
  2・6・10行目だけが違うのに1つと返る。F7 の送りは動くので実害は出ていないが、
  Monaco が3つに分けない理由が分かっていない（2026-09-02 課題化）
- **この Mac ではゴミ箱行きが全部止まっている。** `d`（削除）も同じ `-1743` で、
  `cian-lua` の `a_macro_can_glob_zip_and_delete` が手元でだけ落ちるのもこれ。
  cian のバグではなく、システム設定 → プライバシーとセキュリティ → オートメーション
  の話。**`trash` を `NsFileManager` に切り替えれば権限は要らなくなるが、Finder の
  「戻す」が効かなくなる**ので、削除の挙動を変える話として先に相談した。
  **2026-09-10、本人から「今のままで良い」。** `trash` のまま、Finder の「戻す」を
  取る。この Mac で `d` が効かないのは cian のバグではなく、システム設定 →
  プライバシーとセキュリティ → オートメーションの話なので、そのままにする

---

## ノートは amber へ出た（2026-09-06）

ここには「ノートのモードを本人が普段使いするものにしたい」から始まる長い節が
あった ── Mac と iPhone、Google Drive / Dropbox / iCloud、App Store、そして
**iPhone に Electron は載らない**という一つの事実から出てくる設計の分かれ道。

**それは全部 `~/workspace/amber` に移った。**2026-09-05 にリポジトリを分け、
2026-09-06 に cian の窓から amber モードを外した。同期の話も、Google Drive が
iOS でフォルダを渡せない話も、向こうの `README.ja.md` と
`REQUESTS.moved.ja.md` にある。

**cian に残ったのは、ノートの話ではなく Markdown の話だけ**である ──
`cian-core/src/markdown.rs`（ビューアと `Ctrl+E` のプレビューが読む解釈、
チェックボックスの `set_check` を含む）。ファイラが自分で開いた `.md` を
読むためのもので、ノートアプリの判断ではない。

**cian は2画面ファイラ。** ここから先の道はその形の中にある。

---

## 決めたこと（2026-09-10）

**「端末版の全部を持っていくのか」は、答えが出た。四つとも、もう窓版に入っている。**
この節は長いあいだ「決めていないこと」という題で、マクロ・SSH・AI・git/svn の四つを
未決として並べていた。実際には四つとも入っていて、**節のほうが古かった**。

- **マクロ**（`@` / `q`）── 2026-09-02 に入った。`macro/` ディレクトリも読む
- **SSH / リモートペイン**（SFTP）── 入っている。**心配していたコストは無かった**。
  `cian-scp` は russh で、C のビルドは復活していない。`cian-server` の Cargo.toml に
  「恐れていた数GBは*オフラインの機械でビルドする*ための話で、あの機械には exe が
  届く」と書いてある
- **AI** ── 3つの命令が入っている
- **git / svn** ── どちらもメニューにあり、`parity.py` の免除は0件。git.rs も svn.rs も
  **外部コマンドを叩くだけ**（libgit2 を link しない）なので、バイナリは増えない

**git にどこまで踏み込むかは決めた。**

- **入れた**: 素のコミット（`:commit`、2026-09-10）。それまで git のコミットは
  `:aicommit` の中にしか無く、AI に届かない機械ではコミットできなかった
- **入れない**: push / merge / reset。取り返しのつかない操作を一覧の上から撃てる
  ようにする価値より、誤爆の危険が勝つ。ターミナルは `Shift+J` で下にある
- **保留**: `git pull --ff-only`（svn の `update` に当たるもの）。衝突の解決を
  ファイラの中でやり始めると作りが大きくなるので、要るとなってから

**配布は窓版一本**（2026-09-10、本人）。端末版のコードは残り、テストも通り、
ソースからは建つが、資材としては出さない。`release.yml` から端末版の梱包を落とした。

---

## 次: 設定画面（2026-09-11 に依頼）

**`init.lua` を素人に書かせない。** 本人の言葉:「init.lua を修正するっていうのは
素人にはしんどいと思うんだ」。crmaine の GUI 版に付いている設定画面を参考にする
（`~/workspace/crmaine/gui/settings.js`）。

### 決まっていること

* **正は `init.lua` ひとつ。** 画面は init.lua を読んで表示し、更新・登録・削除も
  **init.lua を直す**。別の設定ファイルを増やして「どちらが勝つか」を作らない
  ── この家で一番多いバグは「設定を直したのに効かない」で、書き手が二人になると
  必ず増える
* **AI は既定オフ。** 設定すれば足せる。endpoint・model・api_version・broker を
  画面から入れる。SSH とスニペットも画面から見て直せるように
* **開き口は右クリックと `Shift+Enter` のメニュー**（2026-09-11、本人）。
  コマンド（`:settings` のたぐい）を覚えないと開けない設定画面は、**設定画面を
  必要とする人には開けない**
* **鍵も `init.lua` に書く**（2026-09-11、本人が判断）。OS の預かり所は使わない。
  もともと `password` も `api_key` もこのファイルに書く作りで、見本自身が
  「平文で保存されます。可能なら `password_cmd` を」と註を付けている ──
  画面はその `password_cmd` の道も見せる（隠すのではなく、選べるようにする）

### crmaine から持ってくる作法（あちらが実機で転けて学んだもの）

1. **項目はスキーマから生成する。** 手で並べると、設定を足した日に取り残される。
   cian では `cian-lua` の `Options` 構造体がそれ ── 型も既定値も説明も doc
   コメントに揃っている
2. **元のファイルに重ねる。丸ごと書き直さない。** crmaine は画面が知っている項目
   だけで JSON を組み直して上書きし、手で足した項目を消し、「書いてある既定値」
   （`useDb = false` は**明示**）まで落として機能を勝手に戻した
3. **読めなかったら保存させない。** 壊れた設定ファイルを既定値で塗り潰して
   5,399 バイトが 64 バイトになった日がある
4. **控えを取ってから、一時ファイル経由で置き換える**
5. **計算で埋めた値は書かない。** 端末固有のパスが設定に焼き付く
6. **カテゴリごとに畳み、最初の1つだけ開く。** 45 項目をフラットに並べない
7. **「設定済み」は、ファイルに実際に書いてあるかで数える。** 読み込み後の値には
   計算した既定が入っていて、何も書いていないのに「1件設定済み」と出る
8. **object の配列を「1行に1つ」で編集させない。** 保存すると設定が壊れる
   （cian では SSH ホストとスニペットがこれ）

### できているもの（2026-09-11 に一通り）

**画面は立っている。** 右クリック / `Shift+Enter` のメニューの「設定…」、
`:settings` でも開く。項目はエンジンが `settings_read` で渡す表から描き、
分類ごとに畳んで最初の1つだけ開く。保存は**触った項目だけ**を
`settings_write` へ送り、`init.lua` に重ねる ── 画面が知らない行は送りようが
無いので消えない。`init.lua` が Lua として読めないときは、赤く出したうえで
**保存を押させない**。

**AI は既定オフ。** `endpoint` が書いてあるかどうかがオンで、別のスイッチを
足していない ── 状態を持つものが二つあると、必ず食い違う。

**鍵は `init.lua` に平文で書く**（本人の判断）。画面の註にもそう書いてある。

### 土台

**`cian-lua` の `settings_edit`**（2026-09-11）── `init.lua` の
`cian.set_option("名前", …)` を、**行ごと外科的に**書き換える。ここを間違えると
人が何ヶ月も育てた設定が消えるので、画面より先に作って証明した。

同梱の見本が全部コメントアウトされている形が、そのまま道になっている:

1. 有効な行がある → 値だけ差し替える（行末の注釈は残す）
2. コメントアウトされた同名の行がある → それを有効にして値を入れる
3. どちらも無い → 末尾の「設定画面が書きます」節に足す
4. 消す → 行を削らず、**コメントに戻す**（説明が消えるから）

同じ名前が並んでいたら**最後の有効な行**を書き換える（Lua は上から実行するので
それが勝つ。見本の `shell` は Windows / pwsh / zsh の3行が並んでいる）。改行は
元のまま（`\r\n` を `\n` にすると git が全行を変更として出す）。変数に入れてから
呼ぶような書き方は**見つからないのが正しい** ── 拾おうとすると書き手の行を壊す
道具になる。見つからなければ末尾に足すだけで、元の行は無傷。

同梱の 406 行の見本を通して、**説明の行が1行も消えないこと**を検査が見ている。

**ブロックも書ける**（同日）── `cian.ai{…}` `cian.font{…}` `cian.ime{…}` の
ような**平たい表**の中の1項目を差し替える（`set_field_in`）。1行に畳んだ
`cian.font{ face = "…" }` も、複数行のも同じ扉。

**コメントアウトされたブロックは、有効にしない。** 1行の設定なら「`-- ` を
外す」が正解だが、ブロックは違う ── 見本には `cian.ai {` が2つ並んでいて
（Azure の例と OpenAI 互換の例）、中にはさらにコメントアウトされた行が混じって
いる。どちらをどう外すかを機械が決めると、**読むつもりだった説明が設定になる**。
有効なブロックが無いときは末尾の節に新しく書く ── 見本は説明として残り、
実際の設定は一か所に集まる。

**SSH ホストも触れる**（2026-09-11）── `cian.ssh{ hosts = { {…}, {…} } }` の
入れ子の配列。ホスト1台に1つのフォーム（名前・アドレス・ポート・メモ）と
「消す」「ホストを追加」。**1行に1つで編集させない**（crmaine の8番目）。

* **書き先は `ssh.lua`。** ①`ssh.lua` に有効な `cian.ssh{}` があればそこ、
  ②`init.lua` にあればそこ（**手で書いた人の場所を勝手に移さない**）、
  ③どちらにも無ければ `ssh.lua` を作る。どちらに書くかは画面に出す。
  **③を `init.lua` にして本人に見つかった**（「あれ・・？ ssh って別ファイル
  じゃなかったっけ？」）── `ssh.lua` は SSH ホストの置き場として用意された
  別ファイルで、そこへ置かずに init.lua を太らせるのは、この分割を無かった
  ことにするのと同じ。新しく作る `ssh.lua` は **600 で作り**、何のファイルかを
  自分で書く（鍵やパスワードを置ける場所だから）
* **`users` は字面のまま運ぶ。** 中に鍵やパスワードが入る
  （`{ name = "ci", password_cmd = "…" }`）ので、畳んで書き直すと形が失われる。
  画面は読めるように出すだけで、直すのは init.lua
* **書き先が誰でも読めるなら、その場で言う。** cian は起動時にも言うが、
  平文のパスワードを置いた人は置いた直後に知れたほうがいい

**画面が触らないものは、画面が場所を言う**（2026-09-11）── 本人の心配
「実装とずれた設定画面になっていないか」への答え。`cian.*` の API は 13 個
あって画面が触るのは3つ、`:where` のファイルは7つ。残りは「ここでは直せない
もの」の節に、**何が・どのファイルに・どうやって**を出す（実ファイルの場所と、
まだ無いかどうかも）。ずれないよう `scripts/settingscover.py` が数え、
出さないものには理由を書く。

### まだ決まっていないこと

* **スニペット**（`cian.snippets{…}`）。同じ依頼に入っていて、まだ無い
* `users` を画面から直せるようにするか（鍵・パスワード・password_cmd の形を
  どう見せるか、が決まっていない）
* 端末版から設定画面を開けるようにするか（いまの計画は窓版だけ）

---

## 毎段の終わりにやること

1. 上の検査 7 つを通す
2. **会社の Windows で実際に触る。** この計画は見た目のために存在するので、
   見ないまま次に進む段は1つもない
3. 依頼を実装したら `gui/REQUESTS.ja.md` に**その場で**1行足す
   （あとでまとめようとすると、まとめる前に自分で消す）
