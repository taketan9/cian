# 依頼の台帳

Taketan が名指しで頼んだこと。**これは装飾ではなく契約です。**

## なぜこれがあるか

2026-08-30、「きちんとアクティブパネルのみ枠線を太くしてほしい」と言われ、
`0eb71b5` で直しました。翌 8/31、見た目を作り直す `951bcd1` で**その行を自分で
削除しました** ── 「枠はカーソルが既に答えている問いへの一番うるさい答え方だ」
というコメントまで添えて。8/31 の夕方、同じ指摘をもう一度受けました。

見落としではありません。**上書きです。**設計パスに入った瞬間、その CSS が
「自分で決め直してよい既存コード」に見えた。そう見えたのは、コードのどこにも
**その行がどこから来たのか書いていなかった**からです。私が自分の判断で書いた行と
区別が付きませんでした。

同じ commit は枠を4つ消していて、うち3つ（シェルにキーがある・同期中・記録中）は
別の signal に載せ替えてありました。**置き換えずに消したのは、頼まれた1つだけ**です。

だからこの表があります。行ごとに「これが今も真であること」を機械で確かめます。
消すなら検査が落ちる。落ちたら、直す前に**訊く**。

## 使い方

```bash
python3 scripts/requests.py          # 全部通るか
python3 scripts/requests.py --list   # 何を見ているか
```

`検査` 欄は `ファイル ~ 正規表現`（マッチすること）か `ファイル !~ 正規表現`
（どこにも出てこないこと）です。

**識別子には終端を書くこと**（`\(` や `\b`）。`~ pub fn foo` は `pub fn foo2` に
そのまま当たるので、名前を変える変異をすり抜けます。2026-09-02 に3回やりました
── 検査を足したら壊して鳴らす、をやっていなければ3回とも気づいていません。

否定は専用の書き方にしてあります。最初は否定先読み `^(?!.*やめる).*$` で書いて
いて、これは「その語を含まない行が1行でもあれば成功」なので**常に通ります**。
3行が何も検査せずに「守られています」と言っていました。足したら必ず壊して、
鳴るのを見てから戻すこと。

## 表

| # | 日付 | 依頼（原文） | 満たすもの | 検査 |
|---|---|---|---|---|
| 1 | 2026-08-30 | きちんとアクティブパネルのみ枠線を太くしてほしい | キーのあるペインに枠。`data-focus` で条件付け。**枠は中身より上に描く**（inset box-shadow だと Windows の実スクロールバーが右辺を覆って消える）。色は `--mode-accent` ＝ `/` `//` は緑・`:` は紫・`v` は橙（端末版 `focus_badge_color`。2026-09-01「対象のファイラパネルを緑っぽい枠にしてほしい」）。**既定は 2026-09-06 にアクセントから固定のシアンへ**（依頼 153）── 枠が言うのは「こちら側にキーがある」の一文で、それは18のパレットで同じ文。**同じ日にシェルの金もやめてシアンに**（依頼 154）── シェルパネルも面の1つで、鍵の在り処を言う色が面ごとに違う理由が無い。**太さは 2026-09-05 に 2px → 3px**（104 番台の依頼103。控えめすぎて目立たない、と） | `gui/index.html ~ (?s)\A(?=.*#work \{ --mode-accent: var\(--cian\); \})(?=.*data-focus="files"\]\s*\.pane\.active::after\s*\{[^}]*border: 3px solid var\(--mode-accent\))` |
| 2 | 2026-08-31 | フォーカス以外はちょっと控えめの配色に | 現在でないペインの一覧・パンくず・見出しを減光 | `gui/index.html ~ \.pane:not\(\.active\)[^{]*\{[^}]*opacity` |
| 3 | 2026-08-30 | 「やめる（Esc）」は日本語が変。キャンセル（Esc）がよい | 確認シートのボタンは「キャンセル」。足のヒントは端末版と同じ「取消」 | `gui/index.html ~ キャンセル` |
| 4 | 2026-08-30 | （同上）足のヒントに「やめる」を残さない | 端末版は取消を43回使い、やめるは1回 | `gui/renderer.js !~ Esc\s*やめる` |
| 5 | 2026-08-30 | 「/」「//」「:」で全部下部にコマンド入力できるように | 3つとも下端の1行に入力する。全画面のモーダルにしない | `gui/renderer.js ~ const PROMPT_SIGN = \{` |
| 6 | 2026-08-30 | 検索系だと緑枠、コマンド系だと紫っぽい枠に | 端末版 `focus_badge_color` の値をそのまま | `gui/index.html ~ --m-command:\s*rgb\(200,\s*100,\s*200\)` |
| 7 | 2026-08-30 | リモートペインの赤枠 | 端末版と同じ carmine | `gui/index.html ~ --m-remote:\s*rgb\(214,\s*45,\s*70\)` |
| 8 | 2026-08-30 | フォントは同梱する | HackGen Console NF を `vendor/fonts/cian.ttf` から窓が自分で読む | `gui/index.html ~ @font-face` |
| 9 | 2026-08-30 | 一覧は端末版の列構成に揃える | 4桁年の日時列 | `gui/renderer.js ~ \$\{d\.getFullYear\(\)\}-\$\{md\}` |
| 10 | 2026-08-30 | shell1 を名称変更できるようにしたい | `:shellname` / `:tabname`。端末版にも入れた | `gui/renderer.js ~ async function cmdShellName` |
| 11 | 2026-08-30 | シェルの初期表示を25%に | 起動時の分割比 | `gui/renderer.js ~ const layout = \{ main: 75` |
| 12 | 2026-08-30 | ゴーストの入力案内は不要なので消して欲しい | プロンプト行に placeholder を置かない | `gui/index.html !~ id="f-input"[^>]*placeholder` |
| 13 | 2026-08-31 | .md でプレビューと普通のテキストを行き来するショートカット | `Ctrl+E`。Monaco が同じ鍵を持つので捕捉フェーズで先に取る | `gui/renderer.js ~ async function togglePreview2` |
| 14 | 2026-08-31 | （vim 流で）Ctrl+C/V/X が効かない | 端末版 `viewer_vim_key` は Ctrl/Alt 付きを文法に渡さない。同じにする | `gui/renderer.js ~ cxvCXV` |
| 15 | 2026-08-31 | ブックマークのフォルダが最初から開いて見える | 端末版 `sc_level` と同じく1階層ずつ | `gui/renderer.js ~ let scPath` |
| 16 | 2026-08-31 | F11 でウィンドウが最大化しない | F11 で全画面。他のキーと同じ場所で読む | `gui/renderer.js ~ async function cmdFullscreen` |
| 17 | 2026-08-31 | :version でビルドの時間は見れないよ | ビルド日時と commit と書体を出す | `crates/cian-server/build.rs ~ CIAN_BUILT_AT` |
| 18 | 2026-08-30 | 会社ではGitコマンドは使えない前提で | Electron の場所は `electron.txt` に1行。zip に入れない（次の版で消えるので） | `gui/run.bat ~ for /f "usebackq delims=" %%L in \("%HERE%\\electron\.txt"\)` |
| 19 | 2026-08-31 | run.bat の文字化けエラー | `chcp` を実行しない。純 ASCII | `gui/run.bat !~ ^\s*chcp` |
| 20 | 2026-08-31 | cian-tui / cian-gui で表記の揺れ・動きの揺れがあれば揃えたい | 端末版の語を窓版のメニュー・スイッチ・並び替えに写し、**機械で確かめる**（`scripts/parity.py`）。CI にも入れた | `.github/workflows/ci.yml ~ python3 scripts/parity\.py` |
| 21 | 2026-08-31 | （同上）機能・挙動の違いをなくしていきたい | ビューアの文法の既定を端末版と同じ vim に | `gui/renderer.js ~ ^let style = 1;$` |
| 22 | 2026-09-02 | コピー・リネーム・移動のあとの Meta+z も戻したい | `Ctrl+z` / `⌘Z` を一覧の取り消しに（`Ctrl+Shift+z` でやり直し）。**コピーも取り消せるようにした** ── そのコピーが新しく作ったものだけをゴミ箱へ | `gui/renderer.js ~ if \(e\.shiftKey\) redo\(\); else undo\(\);` |
| 23 | 2026-09-02 | コピーの取り消しは、そのコピーが作ったものだけ | 先に存在した名前は上書きでも飛ばしでも対象外。判断は1か所（`copy_creates`）で、両前端が同じものを使う | `crates/cian-core/src/ops.rs ~ pub fn copy_creates` |
| 24 | 2026-09-02 | jj 系は端末版にも入れて欲しい | `jj` / `ｊｊ` / `っｊ` で挿入モードを抜ける。`ZZ` / `ZQ` も | `crates/cian-tui/src/viewer.rs ~ pub\(crate\) const JJ_ESCAPES` |
| 25 | 2026-09-02 | アイコンを exe に焼き込むのをやってほしい | `winresource` で `cian-tui.exe` / `cian.exe` / `cian-server.exe` に `cian.ico` を。**無ければビルドを止める**（黙って軽い zip を出した font の再発を防ぐ） | `crates/cian-bin/build.rs ~ res\.set_icon\(icon\)` |
| 27 | 2026-09-02 | （同上）コマンド提案の精度 | **OS ではなく「そのシェルが今どこにいるか」**を渡す ── タイトルの `user@host`／画面に残る `ssh` 行／プロンプトの形、そして `init.lua` の `notes` から**接続先のOS**まで。端末版が持っていたものを窓版にも。一覧とマークも渡し、1行で書けないときは `# ` で断らせる | `crates/cian-server/src/main.rs ~ cian_core::shellwhere::describe\(` |
| 29 | 2026-09-02 | 端末版にあって窓版に無いポップアップを埋めたい | 開いている zip へのコピー（端末版 `ConfirmZipAdd`）。**それまでは zip の隣に落として「コピーしました」と言っていた** ── アーカイブ表示は作り物で、ペインは入る前の `cwd` を覚えたままだった | `crates/cian-server/src/main.rs ~ "zipadd" =>` |
| 30 | 2026-09-02 | リモートの深さを実装してほしい | ローカル↔サーバの `c`/`m`（**それまでは黙って手元にコピーしていた**）・サーバ内の移動・chmod・サーバ間の中継。同一サーバの移動は rename で済ませる | `crates/cian-server/src/jobs.rs ~ pub fn start_remote\(` |
| 31 | 2026-09-02 | （同上）実際に通したい | **127.0.0.1 に自分の sshd を立てて通す**（`scripts/remote.py`）。管理者権限は要らない。これが無かったのでリモートは一度も検証されていなかった | `scripts/remote.py ~ def free_port\(` |
| 32 | 2026-09-02 | （同上）鍵で入れるようにしたい | `cian.ssh{ users = { { key = "~/.ssh/id_ed25519" } } }`。**設定例は前から鍵認証を勧めていたのに、機能が無かった** | `crates/cian-scp/src/lib.rs ~ \.authenticate_publickey\(` |
| 33 | 2026-09-02 | （同上）ディレクトリごと送れるようにしたい | `cian_scp::plan_upload` / `plan_download` が木を歩き、ジョブが親から順に mkdir する。**端末版にも無かった**（両方ファイルだけ送っていた）。リンクは辿らない | `crates/cian-scp/src/lib.rs ~ pub fn plan_upload\(` |
| 34 | 2026-09-02 | Shift+f / Ctrl+f のポップアップの表題が `:find` `:grep` で何をするか分かりづらい | 表題を**何をするか**の文（`about`）に。欄の中に何を打つか（`arg`）、コマンド名は下に小さく残す ── 名前は覚える価値があるので消しはしない | `gui/renderer.js ~ a = await askFor\(cmd\.about,` |
| 35 | 2026-09-02 | 「z」の宛先入力窓の横幅が小さく、入力した文字列が見切れる | パスを訊く窓は広く（`min(60rem, 84vw)`）。380px は**ファイル名の幅**であってパスの幅ではなかった | `gui/index.html ~ #ask \.sheet\.wide \{ min-width` |
| 36 | 2026-09-02 | ビューアに対ディスク差分ガター | 編集中の行のうち**ディスクと違う行**を行番号の脇に出す。判定はエンジンの `cian_core::diff`（窓で二つ目の差分器を書かない） | `crates/cian-server/src/main.rs ~ "diskdiff" =>` |
| 37 | 2026-09-02 | AVD の Program Files に置いて共通デスクトップの run.bat で全員に使わせたい。初回の人には僕の init.lua や ssh 設定を配りたい | `run.bat` が `default-config\` から各自の `~/.config/cian` へ配る。**初回だけ配る**（以後その人のもの。ssh.lua も含めて上書きしない ── 使っている最中に設定を書き換えられる道具は信用できない）。**exe の隣に置く配り方は罠** ── 書き込み先まで Program Files になり、全員のしおりが保存できなくなる | `gui/run.bat ~ default-config` |
| 38 | 2026-09-02 | tar への書き戻し | `tar_modify` ── zip と同じ4つの操作（削除・改名・追加）。tar は差分編集できないので**丸ごと書き直す**が、**新しいものが完成するまで元を消さない**（誰かの唯一のコピーであることがある） | `crates/cian-core/src/archive.rs ~ pub fn tar_modify\(` |
| 39 | 2026-09-02 | ディレクトリ転送に確認シート | サーバへフォルダを送るとき、**中に何ファイル・何バイトあるか**を確認前に出す。数は転送が使うのと**同じ計画器**（`plan_upload`）から取る ── シートと実行が別々に数えると、約束と結果がずれる | `crates/cian-server/src/main.rs ~ "transferplan" =>` |
| 40 | 2026-09-02 | スクリプトマクロを窓版でも | `macro_script::run` をエンジンから呼ぶ。**それまでは「まだ動かせません」と断っていた** ── `macro.lua` は両方の種類を1ファイルに持つので、半分だけ動く状態だった | `crates/cian-server/src/main.rs ~ macro_script::run\(` |
| 41 | 2026-09-02 | （同上）`macro/` ディレクトリも読む | 探索の規則（`macro.lua` と `macro/*.lua`、`.en.lua` は飛ばす）を `cian_lua::macros::load_all` に。**窓版は前者しか読まず、同梱の例のように1ファイル1マクロにしている人にはランチャーが空だった** | `crates/cian-lua/src/macros.rs ~ pub fn load_all\(` |
| 42 | 2026-09-02 | ダークテーマのときもタイトルバーが明るいまま | タイトルバーは OS が描くので CSS では届かない。`nativeTheme.themeSource` を**実際に描かれている `--bg` の輝度**から決めて渡す（配色の名前ではなく）。起動時の地の色もエンジンに訊く ── `main.js` は18ある配色のうち3つしか表を持っていなかった | `gui/renderer.js ~ function tellFrame\(` |
| 44 | 2026-09-02 | ビューは一覧・クラシック・ノートの三つでよい（アイコンは消す） | **入口だけ閉じた** ── 切替バー・メニュー・`:view icons` から外す。描く側は残す: アイコンの当たり判定は詳細ビューのアドレスバー・パンくず・タブ・サイドバーと同じ関数の中にいて、外そうとして三度とも詳細ビューを壊しかけた。**分解は別の日に** | `gui/renderer.js ~ const VIEWS = \[(?![^\]]*icons)` |
| 45 | 2026-09-02 | SharePoint の場所を指定できるようにしたい（crmaine と同じやり方で） | **認証しない。**URL を WebDAV の UNC パスに直すだけ（`\\host@SSL\DavWWWRoot\…`）。crmaine の `sharepoint_to_unc` を写した ── 貼られる4つの形と、**場所を持たない共有リンク**の見分けは高くついた知識なので思い出しで書かない。開けないときは crmaine と同じ手順を出す | `crates/cian-core/src/sharepoint.rs ~ pub fn to_unc\(` |
| 103 | 2026-09-05 | 窓版のアクティブなパネルの枠、もう一段太く。おしゃれだが控えめすぎる | 2px → **3px**。枠の仕事は「探さなくても見えること」で、明るい配色では地の色が数%しか違わないので、枠だけがどちら側にキーがあるかを言っている | `gui/index.html ~ border: 3px solid var\(--mode-accent\)` |
| 112 | 2026-09-05 | Mac でいよいよ使いたい。win-gui / mac-gui に cian モードを | **`cian-gui-macos.zip` を作った。** これまで窓版を Mac に載せる道は「リポジトリを clone して `npm start`」だけで、それは書いた本人には十分でも、人に渡せる形ではない。中身は Windows 版と同じ（front end・vendor 一式・エンジン）で、エンジンは **Intel と Apple Silicon の両方入り** ── 片方だけのバイナリは、もう片方の Mac では「動かない」ではなく**開かない**（何の説明も出ない）。重い方（winit 版・端末版・`.app`）は `everything` の裏のまま。**フォントが無ければビルドを止める** ── Windows 側で一度、黙って 6MB 軽い zip が出ている | `.github/workflows/release.yml ~ name: cian-gui-macos` |
| 117 | 2026-09-05 | T にある アイコン・クラシック・cian の切り替えを Shift+Enter にも。今後めっちゃ使う | 窓が**何であるか**を変える切り替え（ファイラか、タイル壁か、ノートアプリか）なので、よく開くメニューの中に置く ── そこまで頻繁なら、キーが1つ遠いのは遠すぎる。右クリックも同じメニュー。**2026-09-05 に語を「モード ▸」に変えた**（依頼135。クラシックモード／詳細一覧モード／cian モード）ので、検査もそちらを見る | `gui/renderer.js ~ Mode ▸` |
| 118 | 2026-09-05 | シェルパネルで Ctrl+c/x/v を許可。既定の Ctrl+C（中断）は右クリックと Shift+Enter のメニューへ | **この3つだけ。** Ctrl+A は行頭、Ctrl+R は履歴、Ctrl+U は行を捨てる ── それらを取り上げたシェルはシェルではないし、どれも「Ctrl シリーズ」の意味ではない。**流れ去った字は切り取れない**ので Ctrl+X はコピー（黙って何もしないより、切ったふりをして残すよりまし）。中断はメニューの行に | `gui/renderer.js ~ 中断  （Ctrl\+C を送る）` |
| 121 | 2026-09-05 | クラシック・アイコン・cian への遷移はトグルとメニューの最上位に | 窓が**何であるか**を変える切り替えで、その下の全部は「いま居るどれかの中でやること」。だから先頭。ノートの置き場所もその直下に置いた（見せ方を選んで、次にそのノートがどこにあるかを言う、という順） | `gui/renderer.js ~ ── first, because it is the switch reached for most ──` |
| 126 | 2026-09-05 | 表記の揺れ・常体の混在を統一してほしい | 数えたら**常体はほとんど無かった**（747 対 22 で、22 のうち大半はボタンの名前 ── 「削除する」「保存する」は文ではなく命令なので常体が正しい）。**本当に揺れていたのは語の方**で、直したのは4つ: `フォルダー`→`ディレクトリ`／`ノートブックへ移す`→`フォルダへ移す`／`貼付`→`貼り付け`（10か所）／`置き場所`・`保存先`→**`保存場所`**（同じ画面の中で、見出しが「ノートの置き場所」、ボタンが「保存場所を選ぶ」だった）。**残す使い分けは理由を書いた** ── ファイラは硬い漢語（ディレクトリ・新規・選択）、cian モードは柔らかい和語（フォルダ・新しい・選ぶ）で、読む人が違う。「元に戻す」はボタンの名前、「取り消し」は「やらなかった」という報告 | `scripts/audit.py ~ '保存場所':   \['保存場所', '置き場所', '保存先'\]` |
| 127 | 2026-09-05 | （126 の再発防止） | **用語の検査が Rust しか読んでいなかった。** 窓版の `tr()` と電話の Swift は一度も数えられておらず、揺れはそこで育った ── 検査は「揃っています」と言い続けていた。3つの前端すべてを読むようにして、数えて出てきた家族（保存場所・タイトル・貼り付け・作る）を足した。**数えていないものは揃わない** | `scripts/audit.py ~ 3つの前端すべてから` |
| 134 | 2026-09-05 | 窓版の絞り込みも OR / AND に | 一覧の `/` は `query::hits` を通る。**問いの意味は1か所** ── 端末版と窓版が別々に決めると、2語打つまでは一致する検索欄が2つできる。（2026-09-06、amber が出たので判断は `cian-core/src/query.rs` に持ち直した。当時は `note::hits` を借りていた） | `crates/cian-core/src/query.rs ~ pub fn hits\(hay: &str, query: &str\)` |
| 135 | 2026-09-05 | 「一覧の見せ方」ではなく「モード」に。クラシックモード・詳細一覧モード・cian モード | 名前にも付けた ── メニューが「モード ▸ クラシックモード」なら、それがそのまま呼び名になる | `gui/renderer.js ~ 'クラシックモード'` |
| 136 | 2026-09-05 | 詳細一覧モードの上に出る `[左]` はダサい。下に `L` が出ているので不要 | 同じことを2回言っていて、しかも読む場所として悪い方（一番読まれる行の頭）を使っていた | `gui/renderer.js ~ const lead = '';` |
| 149 | 2026-09-05 | モードの並びを一つに揃える ── クラシック → 詳細一覧 → amber | 並びが二つあるのは並びが無いのと同じで、どちらが正しいか言えなくなる。`表示 ▸` だけ「詳細一覧 → cian → …ノートの用事… → クラシック」で、**クラシックがノートの行の下に取り残されていた**。端末版の `menu.rs` も同じ順に直した。クラシックが先なのは既定であり彼が基本にしているから。**アイコンモードは引退のまま**（彼の判断: くどいうえ Windows の OS アイコンにコストを掛けない）。**2026-09-06 に amber が出たので並びは二つ**（クラシック → 詳細一覧）── 約束は「並びは一つ」なので、行そのものは生きている | `gui/renderer.js ~ (?s)▥ クラシック.*▤ 詳細一覧` |
| 152 | 2026-09-06 | シェルパネルで Meta+Shift+矢印による境界の移動ができない（ファイラパネルではできる） | **規則は1つ、扉も1つ。** シェルの受け口が `shellresizepane` を自分で呼んで `stopPropagation` していたので、同じキーを持つ一覧側の受け口に**永久に届かなかった** ── 受け口は `document` の **capture** にあり、capture で止めると同じ節の bubble も走らない。書き写された側には**内側の分割が無いときの落とし先**が無く、結果として四方向とも何も起きなかった。落とし先は「同じ軸の外側の境界」で一つの規則にした（上下＝ファイル｜シェル、左右＝左｜右ペイン）。端末版の `keys.rs resize_split` も同じに揃えた ── **半分だけ効くキーは「このキーはここ用ではない」と読める**が、これは窓の配置についてのキーで、どこで押しても同じ意味 | `gui/renderer.js ~ (?s)mod\(e\) && e\.shiftKey && e\.key\.startsWith\(.Arrow.\)[^}]*resizeSplit\(e\.key\)` |
| 153 | 2026-09-06 | アクティブなパネルの枠は、どんなテーマでも同じ色に。シアンっぽい固定の枠色がいい | **枠が言うのは一文だけ**「こちら側にキーがある」── それは18のパレットで同じ文なので、色が18通りあるのはおかしい。緑（検索）・紫（コマンド）・橙（visual）・金（シェル）・カーマイン（リモート）は最初から固定で、**既定だけがアクセント＝パレット任せの例外**だった。色はアイコンの明るいほう `packaging/icon.py` の `G1 = #16CBE1`（彼が「色味は好き」と言った2色の片方）。**端末版も同時に直した** ── `focus_badge_color(Normal)` が `theme().accent` を返していたので、窓版だけ変えると二つが食い違う。8つのパレットで `--mode-accent` が `rgb(22, 203, 225)` のままであることを機械で確かめた | `crates/cian-tui/src/render.rs ~ Mode::Normal => CIAN,` |
| 154 | 2026-09-06 | シェルパネルに焦点があたったときも、金ではなくシアンに。アクティブパネルはシアンにしたい（検索は緑、コマンドは紫のまま） | **色が答えている問いが2種類あった。**「どちらの面に鍵があるか」と「いま何をしているか」。前者は面が変わっても同じ問いなので、ファイラでもシェルでも同じ色でなければならない ── 金は「シェルという面」を言っていて、問いに答えていなかった。後者（緑＝絞り込み・紫＝コマンド・橙＝visual）はそのまま。窓版は `data-mode="shell"` の行ごと落として既定（`--cian`）に落ちるようにし、`--m-shell` は参照が無くなったので消した。端末版も `focus_badge_color(Mode::Shell)` を `CIAN` に | `gui/index.html !~ data-mode="shell"` |
| 155 | 2026-09-06 | カーソル追従プレビューの既定は、二つの前端で同じにする（窓版に揃えて **切**） | **同じ init.lua が二つの窓を作っていた。** 端末版は `unwrap_or(true)`、窓版は `preview = { on: false }` で、何も書いていない設定だと起動時の画面が割れる。窓版の理由（「探している10秒のために、カーソルが通る全部のファイルを読み続けない」）を採り、端末版をそちらへ。`:preview`・T のトグル・`cian.set_option("preview", true)` で入る | `crates/cian-tui/src/lib.rs ~ preview_on: config.options.preview.unwrap_or\(false\)` |
| 156 | 2026-09-06 | 窓版の `:blame` の欄が、日本語のコミッタ名だと桁がずれる | `padEnd` は UTF-16 の単位で数えるので、`山田太郎` の行は 22 と思って **26 桁**になり、その行だけコードが右へずれて欄がぎざぎざになる（欄の幅は Monaco が1行ごとに決めるため）。表示の桁で切って詰める `padCells` を置いた ── 端末版が `unicode-width` でやっていることの、この窓で要る分だけ。全角の判定は East Asian Width（W/F）で、絵文字も入る | `gui/renderer.js ~ content: padCells\(`  |
| 157 | 2026-09-06 | メニュー「トグル」のインデントがばらついている | ☁ がラベル**の先頭**にあり、その行だけ字の始まりが2桁ぶん右にずれていた（他の行は `│   ` の次から）。印を文の中へ ── `クラウド上（☁）のファイルも読む`。英語版は最初から文の中（`Read ☁ cloud-only files`）で揃っていたので、**日本語でだけ出ていた**。両前端とも同じ言葉なので二つ一緒に動かした（parity が見ているのはそこ） | `crates/cian-tui/src/toggles.rs !~ "☁ クラウド` |
| 158 | 2026-09-06 | init.lua の設定を窓版が黙って捨てているものが無いか、機械で数える物差しが欲しい | **cian の最頻のバグに、そこだけ物差しが無かった。** renderer.js に「窓は cian-tui が読む20個のうち17個を無視していた ── 黙って、というのが最悪」と顛末が書いてあるのに、同じものが3つ残っていた（`tab_width` `editor` `home`）。エンジンが送る `"cfg"` の名前と、窓版の `c.<名前>` を突き合わせる。**免除は「エンジンが自分で使う」という主張**なので、`options.<名前>` が実在するかを裏取りする ── 最初の版は `"cfg"` の中の `cfg.options.lang` を証拠に数えていて、**送っている名前なら何でも免除が通った**（`lang` をでっちあげて確かめた） | `scripts/configcover.py ~ def engine_uses\(` |
| 159 | 2026-09-06 | 窓版が読んでいなかった3つの設定を効かせる | `tab_width` は窓版のもの ── Monaco の**モデル**の `tabSize`（`create` の options ではない。タブ幅は文書の性質）。`editor` と `home` はエンジンのもの: 外部エディタの選び方を `cian_core::editor` に1つ置き（端末版から移した。init.lua が `$VISUAL`/`$EDITOR` より先）、引数の無い起動では `home` を見る ── `main.js` が `os.homedir()` を埋めていたので、`home` はエンジンに届く前に消えていた | `crates/cian-core/src/editor.rs ~ pub fn pick\(` |
| 160 | 2026-09-06 | audit が数え続けていた「同じ行の繰り返し」を括る | `needViewer()` / `needShell()` / `needTargets()` / `sayShellTab()` の4つ。**文言のためではなく、答えが1つであるため** ── 同じ問いに同じ言葉で答える場所が15か所あると、いつか1か所だけ別の言い方になる。真偽を返す形にしたのは `return` と `return null` の両方があるから（中で return するとその違いが消える）。audit ④ が「なし」になり、**初めて「きれいです」が出た** | `gui/renderer.js ~ function needViewer\(` |
| 161 | 2026-09-06 | 端末版にも i18n の物差しを（`tr()` の外にある文字列を数える） | **素朴に数えると 586 件出て、その大半が誤検知だった** ── 端末版は `tr(lang, en, ja)` のほかに `entry(k, a, en, ja)`・`("General", "基本")`・`(…, Lang::Ja) => "…"`・`if ja { } else { }` でも両言語を持っている。「**近くに英語の相方がいるか**」（前後6行）で見ると 8 件。**囲む括弧だけを見る厳密版も試したが、`format!("… 件")` と `msg.contains("中止")`（表示ではなく判定）と `"copying" => "コピー中"` を見分けられず誤検知 23 件** ── 賢くすると別のものを落とす。**見えないもの（訳した行の隣の1行）は docstring に書いた** | `scripts/i18n.py ~ def tui_japanese_only\(` |
| 162 | 2026-09-06 | 端末版で日本語しか言えなかった8か所を英語でも言えるように | デスクトップの帯（戻る／進む／上へ）・表示切替（詳細／クラシック）・左の列（よく使う項目／お気に入り／＋追加）。**`cian.set_option("lang", "en")` を書いた人にも日本語で出ていた面**で、どれも「毎日目に入る面」。切替の検査は日本語だけを待っていたので、**両方の言葉で確かめる**ように直した ── 言語を決めずに日本語を待つ検査は、直したことにも壊れたことにも気づけない。**同じ日の夕方、B-2 でその8か所は面ごと無くなった**（帯も切替も左の列も、端末版では誰も辿り着けないコードだった）。文字列を待つ検査は道連れになるので、**この行が守らせたい規則そのもの**に付け替えた ── 端末版に日本語だけの面を作らない、を `i18n.py` の上限 0 で見る | `scripts/i18n.py ~ TUI_CEILING = 0` |
| 163 | 2026-09-06 | 画面の幅を字数で測っているところを、機械で挙げる物差しが欲しい | 同じ日に**この形のバグが6つ**出て、全部「英語では字数と桁数が同じなので通る」ものだった。数えるのは**道具を通しているか**だけ（Rust は `crate::util::width`、JS は `cellWidth` / `padCells`）。**`slice` は外した** ── 配列にも送る前の打ち切りにも使うので 7 件出て**そのどれも桁の話ではなかった**。当てにならない指摘が並ぶ検査は読まれなくなる。代わりに `.length <= max` の形を1つ足した（`truncateMiddle` がそれ）。**今日の3つを戻すと3つとも鳴る**ことを確かめた | `scripts/widths.py ~ RUST_SUSPECT = re\.compile` |
| 164 | 2026-09-06 | 同じパスを、端末版と窓版が違う長さに詰めていた | 端末版の `truncate_middle` は「全角は2つぶん」で予算を切っていて、窓版の `truncateMiddle` は `text.length` で切っていた ── **日本語のフォルダ名があると窓版は倍近く残し、箱からはみ出す**。窓版を桁で切る形に。実測で `/深い/階層/日本語のフォルダ/…` が 20 桁の指定に対し 19 桁に収まる（前は字数 20 ＝ 桁 40 近く）。`draw_prompt_line` の右寄せも同じ物差しに揃えた（いまは英数字だけだが、日本語を1語入れた瞬間にずれる） | `gui/renderer.js ~ if \(cellWidth\(text\) <= max\) return text;` |
| 165 | 2026-09-06 | アイコン表示（デスクトップ／グリッド／サイドバー）を消す | 「B2 消したい・・。やる」。**画面に出たことがないコードは、間違っていても誰も気づかない**の実例で、`cian-gui`（凍結・削除済み）だけが読んでいた。`icon_view` は真になったことがなく、`skin` は `Classic` 以外になったことがなく、`view_request` は5か所で書かれて**どこでも読まれていなかった** ── その旗を立てるだけの `▤ 詳細 ▥ クラシック` の切替は、押せるのに何も起きない部品として端末版のクラシック画面に出ていた。`native_icons` / `IconSlot` / `ImageSlot` / `Host::Window` / `in_a_window()` も同じ一族で、いずれも「窓が宿主かもしれない」前提。**2507 行を落として 73 行**。窓版からも `viewMode === 'icons'` と `.rows.icons` を外した（入口は 2026-09-02 に閉じてあった） | `crates/cian-tui/src/lib.rs !~ icon_view` |
| 166 | 2026-09-06 | `:view details` / `:view classic` を端末版から外す | 165 の続き。この2つは旗を立てて「窓版のものです」と言うだけで、**端末版には見た目が1つしか無い**。窓版の `:view` は本物（`VIEWS = ['classic','details']`）なので、そのまま残る。`:view` 単独が「カーソルの下を開く」のは前から同じ | `crates/cian-tui/src/commands.rs !~ ViewWanted` |
| 167 | 2026-09-06 | 書体を選べるようにしてほしい（同梱はそのままでよい、僕は Nerd Font にしたい） | 「フォントを縛ることってどうなんだろう」。**縛っていたのは同梱そのものより、同梱以外を選べないこと**だった ── `cian.font{}` は大きさの上げ下げしか無く、書体を名指しする設定がどちらの前端にも無かった。`cian.font{ face = "…" }` を足し、窓版の `--user-face` に差す。**同梱は既定として残る** ── 名指ししたものは列の先頭に立つだけで、後ろの候補は全部そのまま。選んだ書体に字形の無い文字（欧文専用の Nerd Font に日本語）が、機械の既定＝日本語 Windows なら明朝、に落ちないため。端末版は触らない（書体はエミュレータのもの、大きさと同じ理由） | `crates/cian-lua/src/lib.rs ~ pub face: Option<String>` |
| 168 | 2026-09-06 | 「その書体が入っているか」を、当てずっぽうでなく測って答える | 167 を作る途中で出た。**`document.fonts.check()` はこの問いに答えられない** ── *列全体*で描けるかを見るので、どの機械も知らない名前に `true` を返す（実測: `check('16px "Nonexistent Nerd Font XYZ"')` が真）。その上に乗っていた `resolvedFace()` は**列の先頭をそのまま答えとして返していた**ので、`:version` の「書体」欄はずっと嘘だった。同じ文字列を `"<名前>, <総称>"` と `<総称>` で描いて**幅を比べる**形に。総称3つなのは、metrics がたまたま一致する書体を「無い」と読まないため | `gui/renderer.js ~ function faceIsHere\(` |
| 169 | 2026-09-06 | 起動時のあいさつが、一度も画面に出ていなかった | 168 の続き。`refresh().then(…)` に置いてあって、**`fonts.ready` より前・設定が届く前**に走っていた ── 同梱書体はまだ来ていないのでフォールバックを測って「これが書体です」と言い、そのあと `fonts.ready` が呼ぶ `refresh()` に塗り潰されていた。**書いてあって、正しそうに見えて、一度も出ていない。** `greet()` に括り、書体・設定・一覧が揃ってから呼ぶ。3通り（入っている書体・架空の書体・設定なし）を実機の窓で読み返した | `gui/renderer.js ~ Promise\.all\(\[refresh\(\), recall\(\)\]\)\.then\(greet\)` |
| 170 | 2026-09-06 | AI が会話を覚えていなかった（端末版の「チャット」は記録であって会話ではなかった） | C-4 の壁打ちで `cian-ai` を読んで出た。**`cian_ai::chat` が送っていたのは `[system, いま打った1行]` だけ** ── 画面に6往復積み上がっていても、2手目は1手目を聞いていない相手に訊いていた。**画面を見ても絶対に分からない**種類のバグで、「さっき言ったやつ」が他人に答えられていた。`chat_with(cfg, system, prior, user, images)` を足し、端末版は `chat_prior` で渡す（`chat_attachments` と同じ受け渡し ── `ai_request` の時点では新しい質問が既に log に載っているので、popup から読むと二重に送る）。エンジンにも `prior` を通したので窓版は面を作るだけ。**一番古い手から丸ごと落とす**（24000 バイト、半端な手は話者のいない文）が、**最新の手だけは重くても必ず送る** ── それが次の質問の「対象」だから。構造化の依頼（rename/search/commit）は一問一答のまま。python の mock が `[mock +n]` と手数を言うようにして、**運べていることを端まで**確かめた | `crates/cian-ai/src/lib.rs ~ pub fn chat_with` |
| 171 | 2026-09-06 | 窓版に AI の会話窓（C-4） | 窓版の AI は**一問一答で、答えを読むだけのリスト**に出して終わりだった ── 「ではその次は」が言えず、文脈を打ち直すしかなかった。端末版の `Popup::AiChat` と同じものを窓に: 同じ台詞（`あなた` / `AI - simple`、parity が数えている）、同じ6つの入口（`:ai`・要約・エラー説明・差分説明・ログ診断・選択範囲の三つ）が全部この面に着く。**自分のシートにした**（`#report` は入力が上の「絞り込み」で、こちらは下の「発言」。曲げると全部のキー処理に `if` が増える）。**z-index は自分の番号**（`#view` と `#report` が同じ 10 で文書順に負け、シートがエディタの裏に開いた事故があるので）。キーは document の1か所で、節に受け口を足さない ── `stopPropagation` で4ラウンド溶かした形 | `gui/renderer.js ~ function setChatOn\(` |
| 172 | 2026-09-06 | 見せる文と送る本文を分ける（`sent`） | 171 を作っていて出た。`:ailog` は画面に `Triage the log: access.log` と出して、本文はログを送る ── **会話を運ぶようになった途端、続きの質問が「見たことのないファイルの名前」を持って行くようになった**。170 と同じバグの、狭い場所での再発。本文を画面に出せば読めない 一覧になるので、手が両方を持つ（`ChatMsg { text, sent }` / 窓版は `{ text, sent }`）。**端末版の5つの入口すべてに付けた** ── 要約・エラー・差分・ログ・選択範囲の三つ。古い履歴が読めるよう `#[serde(default)]`。**同じ日のうちに置き場所が `cian-core` へ動いた**（173 で履歴を両前端で1つのファイルにしたとき、行の形ごとそちらへ移した）ので、この検査もそこを見る | `crates/cian-core/src/chatlog.rs ~ pub fn for_model\(&self\)` |
| 173 | 2026-09-06 | AI の履歴を、両前端で1つのファイルに | 窓版に会話が入った時点で、放っておけば**履歴が2つに割れる**ところだった ── 端末版は `ai_history.json` に書き、窓版はその存在を知らなかった。**履歴を開く理由は「前に訊いたこと」を探すことで、「前」はどちらのビルドだったかを知らない。** 行の形（`Turn`・`Skin`・`Stored`）とファイルの読み書きを `cian-core/src/chatlog.rs` に一本化し、端末版はそれを `use` する（`ChatMsg` は別名になった）。エンジンに `aihistory`（list / load / delete / save）を足して窓版もそこを読む。`mode` を enum ではなく**文字列**にしたのは、知らないバックエンド名の会話でも**読めはする**ため。**窓が実際に書いたバイト列を端末版のテストに貼った** ── 手で作った標本は、この形に合うように作れてしまう | `crates/cian-core/src/chatlog.rs ~ pub fn remember\(` |
| 174 | 2026-09-06 | 窓版の会話を Esc で中断できるように | 端末版の `cancel_ai_pending` と同じ規則 ── 1度目の Esc は**待つのをやめて会話は開いたまま**、2度目で閉じる。答えが遅いのが Esc に手が伸びる理由なので、そこで一覧まで失うのは罰が重い。**python のワーカーは止められない**（エンジンが既に渡した子プロセスで、割り込む口が無い）ので「待つのをやめた」と言う。ヒント欄の Esc の字も `pending` に追従させた ── どちらの Esc が来るかを間違えて出す欄は、会話を失う鍵を案内する欄になる | `gui/renderer.js ~ if \(chat\.pending\) \{` |
| 175 | 2026-09-06 | 窓版の会話に画像を貼れるように | 端末版は Alt+V（端末が Ctrl+V を文字に使うから）。窓は `paste` イベントで受ける ── 窓の Ctrl+V は「クリップボードにあるものを置く」で、それが絵のこともある。端末の都合を、その都合が無い場所に写す必要はない。窓 → base64 → エンジンが一時 PNG に書く（helper はパスを読む。端末版と同じ入口にするため）。**base64 の復号は手で書いた** ── オフラインの Windows 用に全クレートを vendor しているので、1つの関数のために再 vendor と回線待ちを起こしたくない。RFC 4648 の公表ベクタと 0x00〜0xFF の往復で確かめてある。実機で貼って、エンジンが書いた PNG が**元とバイト一致**することを見た | `crates/cian-core/src/chatlog.rs ~ pub fn from_base64\(` |
| 176 | 2026-09-06 | 端末版がフォルダを黙って落としていたのを直す | `ssh.rs` の2か所に 「ファイルだけ残す」フィルタ があり、**フォルダだけをマークして 送る ▸ アップロード を押すと「アップロードするファイルを選んでください」**と言われた ── 選んだのに。窓版は `plan_upload` で木を歩いて送れていたので、**同じプログラムの半分ずつが「これを送る」の意味で食い違い、端末版はそれを人のせいにする言い方で出していた**。転送そのものも `plan_upload` に通した（SFTP に再帰 put は無いので、木を歩いて親から順にフォルダを作る）。chmod は**マークした物**単位なので、フォルダに付けたモードはその中の全ファイルに掛かる。**この動きを固定していたテストがあった**（`scp_upload_without_a_selected_file_is_refused`）── しかもカーソルが `..` の行に乗っていて、フォルダを試すつもりで親の行を断っていた。名前で選ぶように書き直した | `crates/cian-tui/src/ssh.rs ~ cian_scp::plan_upload\(local` |
| 177 | 2026-09-06 | コピーでタイムスタンプを保存してほしい | **コピー先が「コピーした瞬間」の日付になっていた。** `cp -p`・robocopy・エクスプローラ・Finder・あふ、どれも元の日付を渡す ── 日付はコピーされる物の一部で、バックアップを読む手掛かり・どちらが新しいかの答え・納品物の日付そのものだからだ。しかも気づけない: `dirdiff` は内容で比べるので **cian 自身の比較が意図的に mtime を無視する**。`cian_core::ops::copy_times` に一本化し、ファイルは `progress::copy_file` が書いた直後に、**ディレクトリは木を写し終えてから深い順に**（中に書くたび親の mtime が動くので、先に押しても消える）。`ops::transfer_one`（`:cp` と比較画面の `>`）は転移の**前**に `times_of` を取る ── 別ボリュームへの移動は copy+delete に落ちて元が消えるから。SFTP も両向き（`set_metadata` / `filetime`）。`File::set_modified` ではなく `filetime` なのは、前者が書き込みで開ける必要があり Unix のディレクトリでは不可能だから | `crates/cian-core/src/progress.rs ~ crate::ops::copy_times\(src, dst\);` |
| 178 | 2026-09-06 | 上書き確認に「うち N 件が衝突」を出す | 確認は送る物と行き先を並べるだけで、**`y`（同名はスキップ）と `a`（上書き）のどちらを押すかを、衝突が 1 件なのか 30 件なのか 0 件なのかを知らずに決めさせていた。** 2画面ファイラで一番よく打つ操作がこれ。件数の一行と、衝突する行の **↑ 印**（印の付いた行を先頭に並べ替える ── 20 番目に埋もれた衝突こそ見せるべきもの）。行はパスではなく**共通の親から下**の名前にした: 長いパスは折り返して `↑` が名前の 1〜2 行上に離れ、印として機能しなかった。端末版は `ops::clashes` を `transfer_popup` の1か所で数え、窓版は**行き先ペインの一覧そのもの**から数える（画面に出ているものと食い違えない） | `crates/cian-core/src/ops.rs ~ pub fn clashes\(` |
| 179 | 2026-09-06 | コピーをやり直せるように・undo とディレクトリ移動を分ける | **コピーの取り消しは片道だった** ── `u` がゴミ箱へ送り、`Ctrl+R` は「やり直せる操作はありません」。理由は「中身を覚えていない」で、それは *作った* ファイル（`Created`）には正しく、コピーには当てはまらない: **元は動いていない**。`srcs` と `dest` を段に持たせ、やり直しは同じコピーを走らせる（`Conflict::Skip` ── 取り消しが取ったのはこのコピーが作った物だけなので、いまその名前に居る物は他人の物）。`copy_creates` は**コピーの前**に取り直す（後では全部存在していて空になる）。あわせて **ディレクトリ移動を undo スタックから外した** ── `u` は「いま自分のファイルにしたことを取り戻す鍵」で、あいだの `cd` が一手ずつ挟まると届かない（コピーを取り消した直後の `u` がペインを動かすのを実際に見た）。履歴は `Alt+←` / `Alt+→` が持ち場 | `crates/cian-tui/src/actions.rs ~ UndoAction::Copied \{ srcs, dest, \.\. \} => \{` |
| 180 | 2026-09-06 | AI の要らない機能を刈り込む | 21 綴り・11 動作あり、コマンドパレット 49 行のうち **10 行が AI** ── 既定でオフの機能が常設の画面の 2 割を占めていた。落としたのは**ファイルへの操作を提案する**側だけ（`:aijunk` `:aiorganize`/`:aistructure` `:airename` `:aisearch`/`:semsearch`/`:ask`）: `:airename` は `:renamepattern` と `:renamelist` と、`:aijunk` は `:duplicate` と正面から競合していて、後者はどれも決定的・オフライン・確認付きで**明らかに上等**。残したのは**訊くと答える**もの（`:ai` `:aicmd` `:aicommit` `:aierror` `:aidiff` `:ailog` と F3 の要約）。両前端・エンジン・プロンプト・パーサ・レビュー用ポップアップ2つ・`by_ai` の旗まで消し、誰も呼ばなくなった `cian-core/src/survey.rs`（398行）も落ちた | `crates/cian-tui/src/palette.rs !~ "airename"` |
| 181 | 2026-09-06 | シェルパネルで Shift+←/→ で範囲選択したい | **シェルはマウスでしか選べなかった。** ドラッグは手が既にそこにある時だけ楽で、シェルから抜き出したいのは大抵パスかエラー行 ── つまり**キーに手を置いて読んでいた**ものだ。シェルの**自分のカーソル**から始め、**行をまたいで折り返す**（列 0 で止まると、カーソルはプロンプトの行に居るので出力に永久に届かない ── ここが唯一間違えられる所で、`next_cell` の単体テストが見ている）。`Shift+↑`/`↓` は取らない: 前からスクロールバックを歩いている。`Ctrl+C` がコピーする（ドラッグと同じ `ready` を立てる）。**Esc は選択だけ落としてシェルに留まり**、2度目で出る。窓版は面が HTML なのでブラウザの `Selection.modify` を同じ規則で使う | `crates/cian-tui/src/keys.rs ~ fn extend_shell_selection\(` |
| 182 | 2026-09-06 | 管理者で起動しているのに共有 SH / UNC でコピーが拒否される | **cian が持っていた唯一の答えが空だった。** 拒否されたときの手は「管理者としてやり直す」＝`Start-Process -Verb RunAs` だけで、**既に管理者で起動している人には同じトークン・同じ ACL・同じ拒否**が返るだけだった。エクスプローラのあの確認は昇格を求めているのではなく「管理者の力で ACL を押し通すか」を訊いていて、押し通し方は2つある ── **(A) `SeBackupPrivilege`/`SeRestorePrivilege` で読み書きし、権限は一切変えない**（だから毎回訊かれる）と、**(B) DACL に ACE を足す**（恒久的に変わる。他のメンバーが居る SH では*その全員*への変更）。本人の観測「毎回聞かれる・権限は変わっていない」で **(A) と確定**したので (A) を実装。`windows-sys` は入れず **`robocopy /B`** を直接呼ぶ（バックアップモードはこれそのもの。木・長いパス・UNC を枯れた実装が持つ。`CreateFileW` 版は `CreateDirectoryW` にバックアップ指定が無くディレクトリを作れず、しかも手元で型検査できない）。`/COPY:DAT` で日付は運び **`S`/`O` は付けない** ── ACL や所有者を写すのは、この機能が避けている当の権限変更。確認は `b`（権限は変更しない）と `y`（昇格）の2択になり、失敗報告の助言も「管理者で起動してください」から `b` の案内に変えた。**特権は機械を越えない**ので UNC 先の可否は向こうの ACL 次第 ── 実機での確認が要る。**確認で選んだ答えは再試行にも効く** ── robocopy の既定は「違うものは写す」なので、`y`（同名はスキップ）を選んでいたら `/XC /XN /XO` を渡す。渡さなければ、スキップと答えた当のファイルを黙って上書きしていた | `crates/cian-core/src/backup.rs ~ args\.push\("/B"\.into\(\)\);` |
| 183 | 2026-09-06 | `b` は絶対押さない。エラーのあと「robocopy を実施しますか？」を出して、はいのときだけ動かす | **正しい。** 失敗が上げるダイアログは、語彙を要求してはいけない ── `b` は「その問題を既に理解している人」しか押さない鍵で、それは案内ではない。1問1答にした: `c`/`m` が拒否されると、**何件・どこへ・機械が何と言ったか**を出し、「管理者の権限で robocopy を実行してやり直しますか？ 権限（ACL）は変更しません」を訊く。**はい（Enter）でだけ robocopy が走る。** 昇格は**同じ画面に並べず**、robocopy でも通らなかった *後* にだけ2枚目として出す ── 昇格が直せる唯一のこと（cian が管理者で起動していない）は、robocopy がその場で言ってくれることだからだ。`ConfirmElevate` は `ConfirmRetry { how: Backup / Elevate }` に改名（`Elevate` という名前が主でなくなった以上、その名前は嘘になる）。確認で選んだスキップ／上書きも画面に出す ── 2枚目が1枚目の答えを黙って変えて見えないように | `crates/cian-tui/src/lib.rs ~ pub\(crate\) enum RetryHow \{` |
| 184 | 2026-09-07 | 一覧を F12 で広げたまま Shift+J すると、キーはシェルに行くのに画面が一覧のまま | **`display:none` の面に打ち込んでいた。** 窓版は「どの面を広げたか」を **F12 を押した瞬間に凍らせて**いて（`dataset.zoom = 'files'`）、その後フォーカスが動いても書き換えなかった。端末版は `zoomed` が **bool** で `draw_zoomed` が**毎フレーム `focused` を見る** ── 広げるのは「どの面か」ではなく「いま鍵のある面」。窓版もそれに合わせた: 状態は `zoomOn`（bool）1つで、`dataset.zoom` は `markFocus()`（キーの在り処を書く唯一の場所）が `zoomOn × focus` から**毎回書き直す**従属値にした。`setShellFocus` からも `afterZoom()` を呼ぶ ── 現れた面は自分の大きさを知らされていない。**見えているかは計算後の `display` でしか言えない**（`hidden` でもクラスでもない）ので、検査もそこを読む | `gui/renderer.js ~ el\.work\.dataset\.zoom = zoomOn \? where : '';` |
| 185 | 2026-09-07 | Release に `cian-server-win-x64.exe.zip` を足す（中身は exe 1つ・名前そのまま・生の exe も残す） | crmaine の配布先（会社の運用端末）で**社内の網が `.exe` の直接ダウンロードを止め**、Release から本体を落とせなかった。zip なら通る。crmaine は Release の写しを持つのをやめた（写しは「cian だけ古いまま上がる」日を作る）ので、**cian の Release に無いものは会社端末に届かない**。v3.0.1 には公開済みの exe **そのもの**から作った zip を後から足し（取り出してバイト一致を確認）、`SHA256SUMS` は既存 4 行を一字も変えずに1行追記。以後は `release.yml` が Windows / macOS の両方で自動生成する（`Compress-Archive` と `zip -X` ── `-X` は macOS の拡張属性と `__MACOSX` を混ぜないため。「中身は1つだけ」が守れない）。`SHA256SUMS` は `sha256sum cian-*` なので zip も勝手に載る | `.github/workflows/release.yml ~ cian-server-win-x64\.exe\.zip` |
| 186 | 2026-09-07 | 「Asset にプッシュして」で毎回同じ手順になるようにしてほしい | 185 番で自動化したのは**次の版から**の話で、**既に出た版に後から足す手順はどこにも無かった** ── 私の記憶にも、リポジトリにも。今日踏んだ7手のうち **①公開されている資材を落とす（作り直さない）／④取り出してバイト一致を確認／⑦上げた後に落とし直して照合** の3つは、**飛ばしても成功したように見える**。飛ばすと「名前は合っているが中身が違う資材」を公開し、気付くのは会社端末で困っている人になる。`scripts/release-asset.py` に7手を1本にした。`--dry-run` は上げる直前で止まり、`--self-test` は網に出ない部分（zip の中身・`SHA256SUMS` の継ぎ足し）を**壊して鳴らすところまで**見る。既にある資材の差し替えは**断る** ── それはこの道具の仕事ではない。手で作り直した zip を置く道が無いのが要点 | `scripts/release-asset.py ~ def zip_holds_exactly\(` |
| 187 | 2026-09-07 | `:remote` で繋いだ直後のリモートペイン（サーバのホーム）に `..` が出ない。深い階層に入ると出る。ローカルと同じく常に出してほしい（`/` のときだけ出さない） | **`..` が並べ替えと隠しファイル除外の中に居た。** サーバ版は `..` を普通の行として一覧に混ぜていて、`Pane` がそれをディレクトリとして名前順に並べ、隠しファイル非表示なら `.` で始まる名前として落としていた。名前順でも `-` で始まる名前は `..` の前に来る。逆順（新しい順）ならディレクトリ群の**最後**に沈む ── ホームにはドット付きのディレクトリが 30 個あり、深い階層には 2 つしか無い。それが「ホームでは出ず、深い階層では出る」の正体。`cian-core` の `apply_filter` が `..` を並べ替え・隠しファイル・フィルタの外に出して**先頭に固定**し（アーカイブ内の `..` も同じ扉を通る）、サーバ版の `remote_rows` は端末版と同じく `/` では `..` を作らない。カーソルもローカルと同じく `..` の次から始まる。`scripts/remote.py` が本物の sshd で「逆順でも先頭・`/` には無い」を見る | `crates/cian-core/src/lib.rs ~ else if let Some\(up\) = up \{` |
| 188 | 2026-09-07 | 鍵を設定してあるユーザで `:remote` を開くと、要らないパスワード欄が一度出る。鍵か password / password_cmd があるなら訊かずに繋ぎ、鍵が断られたときだけ聞いてほしい | **窓が「入る物がある」を password と password_cmd だけで数えていた。** `key = "~/.ssh/id_x"` のユーザは欄を出され、空のまま Enter すると鍵で通った ── 押して消すことを覚える欄は、無い欄より悪い。エンジンは前から鍵を先に出しているので（README もそう書いてある）、数え方だけが食い違っていた: `stored` を `has_secret()`（鍵を含む）にし、鍵が断られた1つの失敗だけは文ではなく型で返して（`cian_scp::KeyRefused` → `need_password`）、**そこで初めて**訊く。どの鍵が断られたかも言う ── 直すのに要るのはその一語。ピッカーの印は「鍵あり」を**保存パスワード**に付けていたので、`鍵` と `保存` に分けた。端末版も同じ穴で、鍵だけのユーザは転送を断られていた。本物の sshd で通した（`scripts/remote.py`） | `crates/cian-server/src/main.rs ~ (?s)(?=.*"stored": u\.has_secret\(\),)(?=.*"need_password": true,)` |
| 189 | 2026-09-10 | 普通のコミットを追加していい | Git ▸ に「コミット」、`:commit` で自分の書いたメッセージでコミットする。**AI を通さない道**（それまで git のコミットは `:aicommit` の中にしか無く、社内の endpoint に届かない機械ではコミットできなかった）。訊き方は svn のコミットと同じ入力欄。**2026-09-20、検査から `about:` の1語だけ外した** ── 依頼211で `:svncommit` を別名に足したので `name: 'commit', alias: […], about:` になり、隣の綴りに固定していた regex が外れた。約束（AI を通さないコミットが `:commit` にある）はそのまま3つの条件で見ている | `gui/renderer.js ~ (?s)\A(?=.*name: 'commit',)(?=.*value: ':commit', run: cmdCommit \},)(?=.*async function cmdCommit\(\) \{)` |
| 190 | 2026-09-11 | init.lua の内容を設定画面に読み込んで表示し、更新・登録・削除も init.lua を直す形にしてほしい。init.lua を手で書くのは素人にはしんどい。カーマインの設定画面の作りを参考に。**開き口は右クリック、または Shift+Enter のメニュー** | **正は `init.lua` ひとつ。** 設定ファイルを増やせば「どちらが勝つか」が生まれ、cian で最頻のバグ（設定を直したのに効かない）に水源を足す。項目は `cian-lua` の `Options` から生成し（`settings_schema`、**`Options` と1対1であることを検査が見る**）、保存は `settings_edit` が**行ごと外科的に**書き換える ── 有効な行は値だけ差し替え、コメントアウトされた同名の行は有効にし、無ければ末尾の「設定画面が書きます」節へ、消すときは行を削らず**コメントに戻す**。同梱の 406 行の見本を通して**説明が1行も消えないこと**をテストが見ている。crmaine の `gui/settings.js` が実機で転けた8つ（元のファイルに重ねる／読めなければ保存させない／控えを取って一時ファイル経由で置き換える／設定済みはファイルに書いてあるかで数える…）をそのまま持ってきた。AI は**既定オフ** ── `endpoint` が書いてあるかどうかがオンで、別のスイッチを足していない | `gui/renderer.js ~ (?s)\A(?=.*label: tr\(\"Settings\\u2026\", '設定…'\), value: ':settings', run: openSettings)(?=.*name: 'settings', alias: \['prefs'\])(?=.*async function openSettings\(\))` |
| 191 | 2026-09-11 | SSH も設定画面から見たり設定したりできるように（入れ子の配列） | SSH の節にホスト1台ずつのフォーム（名前・アドレス・ポート・メモ）と「消す」「ホストを追加」。**1行に1つで編集させない** ── crmaine が「object の配列をそう編集させると保存で設定が壊れる」と書いていたのがこの形。**書き先は `ssh.lua`** ── `ssh.lua` に有効な `cian.ssh{}` があればそこ、`init.lua` にあればそこ（手で書いた人の場所は移さない）、どちらにも無ければ `ssh.lua` を **600 で作る**（どちらに書くかは画面に出す）。`users` は鍵やパスワードの形を保つため**字面のまま運び**、画面では読めるように出すだけ。書き先が誰でも読めるなら、その場で言う | `gui/renderer.js ~ (?s)\A(?=.*ask\('settings_hosts_read')(?=.*ask\('settings_hosts_write', \{ hosts \}\))(?=.*class=\"sdrop\" data-hdrop=)(?=.*settings\.newHosts\.push)(?=.*<code>\$\{esc\(h\.users\)\}</code>)` |
| 192 | 2026-09-11 | keymap.lua を設定画面から変更したい。よくあるアプリは自分の中でキーマップを見直しできる | キー割当の節に、縛れる動作 49 個ぶんの行。**綴りを打たせず、押させる** ── 欄を押すと待ち受けになり、次の打鍵がそのまま割当になる（`ctrl+g` のような修飾つきも。1文字にならないキーは受け取らないと**言う**）。**足すのであって置き換えるのではない** ── `keys.rs` は人が縛ったキーだけ先に見て、無ければ既定に落ちるので、「縛らなかったキーは今までどおり動きます。既定の一覧は ? にあります」と一言目に書く。既定のキーは画面に**持たない**（持つと `keys.rs` の match と二つ目の真実ができて必ずずれる）。書き先は `keymap.lua` → `init.lua` → `keymap.lua` の三つの規則。動作の表は `cian-lua` にあり、**`theme::action_from_name` と1対1であること**を cian-tui の検査が見る | `gui/renderer.js ~ (?s)\A(?=.*function captureKey\(e\))(?=.*settings\.capturing === node\.dataset\.cap)(?=.*ask\('settings_keys_write', \{ keys \}\))(?=.*data-kdrop)` |
| 193 | 2026-09-11 | スニペットも設定画面から見たり設定したりできるように | スニペットの節に1つずつのフォーム（名前・コマンド・すぐ走らせるか・送る前に訊くか）と「消す」「スニペットを追加」。**同梱の見本には一行も無かった** ── マニュアルは名前を挙げているのに書き方を知る道が無かったので、この画面がその穴を埋める。分割ファイルは無いので置き場は `init.lua`。**書いてあった既定値は、既定と同じでも書き戻す**（crmaine が実機で踏んだ形 ── 「書いてある既定値」は明示で、落とすと機能が勝手に戻る）| `gui/renderer.js ~ (?s)\A(?=.*ask\('settings_snips_read')(?=.*ask\('settings_snips_write', \{ snips \}\))(?=.*id=\"se-addsnip\")(?=.*data-sdrop)` |
| 194 | 2026-09-12 | AI 独特の表現 ──「題」「釦」「綱」「たったの2つです」みたいな違和感のある言い方の洗い出しとリライト。検査の用語は不要、実際のユーザが見えるところだけでよい | 画面に出る日本語 1878 本を端から読んで直した。**直す先は `tr()` の中だけ** ── コメントや検査の言葉は触っていない（本人の指定）。出たのは一字の漢語だけではなく、**その人が声に出して言わない言い方**全般だった: `窓版`→`ウィンドウ版`（画面の他の3か所は既に「ウィンドウ版」だった）、`見捨てます`→`強制終了します`、`この機械`→`このパソコン`、`縛ったキーが先に見られます`→`割り当てたキーが先に効きます`、`上から順に走ります`→`実行します`、`d 忘れる`→`d 削除`、`結果を言います`→`表示します`、`不可`/`未対応`/`据置`/`未記憶`→ふつうの言い方、`入力方式がこのキーを握っています`→`使っています`、`立っている側`→`カーソルのある側`、`向こうの書いたもの`→`外で書き換えられた内容`。**直した先を `kotoba.py` の④の辞書に鍵として入れ**、天井 0 で CI に入れた ── 戻したら鳴る（窓版・見捨て・不可 の3つで変異テスト済み）。**`tr()` の外にも画面の言葉があった** ── 設定画面の見出しと補足は `cian-lua/settings_schema.rs`、窓版の断りの半分は `cian-server` が書いていて、どちらも検査の外だった（`zip へはコピー（追加）のみ` は三つ目の写しがエンジンに残っていた）。いまは同じ門から読む。**二巡目は本人が一つずつ名指しした**: `打って絞る`→`フィルタ`（15か所）、`未適用`→`まだ切り替えていません`、`メモ帳`→`notepad`（並びも `notepad / vim` に）、`着せ替わります`→`選ぶとその場で変わります`。`半角ブロック` は「そのままでいい」なので辞書に入れない。`Ctrl+D=拾った断片` は**言葉ではなく死んだ案内**だった ── crmaine の橋と一緒に消えた `:ragdebug` を呼ぶキーで、受け口はもう無い（`Char(_) if ctrl => {}` が飲む）。案内ごと落とした。**三巡目は名前の揺れ** ── 同じ `Ctrl+S` の機能に `入力同期`／`同時入力`／`同期入力` の3つ、`T` の画面に `各種スイッチ…`／`トグル`／`UIトグルメニュー` の3つがあった。**`parity.py` は端末版と窓版の「間」しか見ないので、両方に3つとも在るこれは素通りしていた。**本人が選んだのは `シンクロ入力` と `トグル`。キーヒント帯は `入力｜フィルタ` に揃えた（足元の `入力で絞込` も）| `scripts/kotoba.py ~ (?s)\A(?=.*"窓版": "ウィンドウ版")(?=.*"見捨て": "強制終了")(?=.*"この機械": "このパソコン")(?=.*"縛ったキー")(?=.*"走らせ")(?=.*LUA_FILES = )(?=.*SRV = ROOT)` |
| 195 | 2026-09-12 | Windows で cian を叩くのに run.sh（run.bat）を通しているが、exe を叩いて起動できないか。やっぱり exe を叩かせるのがいちばんやりやすい。大きさも台あたり 500MB も払える | **Electron ごと同梱して `cian.exe` をダブルクリックで開く形にした。** 作り方は crmaine と同じ**コピー方式**で、electron-builder は使わない（取得を伴う道具は途中で落ちると中途半端な生成物を残し、それが正常に見える ── crmaine の `gui/pack.js` に書いてある）。`gui/pack.js` が Electron を写して `electron.exe` → `cian.exe`、アプリは `resources/app/`、エンジンも同梱、アイコンは **rcedit** で exe に焼く（crmaine と同じ道具）。**出口で検証する** ── Monaco・同梱フォント・エンジン・`default_app.asar` の除去、そして **`resources/app` に .lua が紛れていたら落とす**（exe の隣の設定は読み書き両方で優先されるので、1つ紛れると全員のしおりが書けない場所へ行く）。**初回だけ設定を配る仕事は `run.bat` から `main.js` に移した** ── `cian.exe` は run.bat を通らないので、入口ごとに書かずに中へ1つ。前面だけの zip と `run.bat` も残す（社内の Electron を共用する道）| `gui/pack.js ~ (?s)\A(?=.*\$\{NAME\}\.exe)(?=.*rcedit)(?=.*default_app\.asar)(?=.*設定を exe の隣に置かない)` |
| 196 | 2026-09-14 | `:theme` を選んで終了しても、次の起動でテーマが永続しない（実機・Windows の同梱版）| **消し忘れだった。** 実機の state.toml が `theme = "nord"` と `gui_look = "hakuji"` の同居で、起動時は「見た目が白磁なら覚えている配色を当て直す」ので毎回 nord（暗い）に戻っていた。**白磁・陰翳・端末譲りを選んだときに、覚えていた配色を消していなかった** ── 逆向き（配色を選ぶと `gui_look` が白磁に戻る）は入っていたので、片側だけの穴。`setLook` で `theme` を空にする（`state_get` は空を「無い」と読む）。`palette` を落とすのも `setLook` に移した ── `:theme 陰翳` は `pickTheme` を通らず、`●` が配色の行に残っていた。**書けない疑いは空振り**（Mode は `-a-----`、`HOME` は空で `USERPROFILE` 一本）。ただし `state_set` が黙って捨てる作りだったので、書けなかったときは理由を言うようにした | `gui/renderer.js ~ (?s)\A(?=.*function setLook)(?=.*key: .theme., value: '' \})(?=.*palette = null)` |
| 197 | 2026-09-14 | F12 で最大化しているとき、マウスで各パネルの大きさを変える機能が生きている。最大化中はオフにしてほしい（窓版・実機）| `placeGrips` は縦の掴み手を「ペインが1枚のときだけ」隠していて、**F12 の最大化を見ていなかった** ── シェルを広げると `#panes` は `display: none` なのに、その上を縦の掴み手が横切っていた。見えない境界を動かせると、戻したときに知らない割り方になっている。`zoomed` を見て両方隠す。あわせて `afterZoom`（埋めている面が変わったときの1つの扉）から `placeGrips` を呼ぶ ── `zoomFocused` は掴み手を置き直していなかったので、隠す判定を足すだけでは F12 の瞬間に効かない | `gui/renderer.js ~ (?s)\A(?=.*const zoomed = !!el\.work\.dataset\.zoom)(?=.*gripPanes\.hidden = zoomed)(?=.*function afterZoom[^}]*placeGrips\(\))` |
| 198 | 2026-09-14 | 枠の太さはファイラパネルとシェルパネルで同じ？ シェルパネルの枠が若干細く見える（窓版・実機）| **細かった。** ペインの枠は 3px、シェルの枠は 2px。ペインの方は「2px では上品すぎて見落とす。枠の仕事は探さなくても見えること」という理由で後から 3px に上げてあり、**シェル側を一緒に上げ忘れていた**。枠は「いまキーがどちらにあるか」を言う道具なので、面ごとに太さが違うと、同じことを言っているのに太い方が強く見える。3px に揃えた（`getComputedStyle(…, "::after")` で両方 3px を確認）| `gui/index.html ~ (?s)\A(?=.*focus="shell"\] #shell::after \{[^}]*border: 3px solid var\(--mode-accent\))` |
| 199 | 2026-09-14 | シェルパネルで F11 を押しても最大化しない。シェルの Fxx は実装されている？（窓版・実機）| **F11 だけ抜けていた。** シェルの受け口には F1/F2（タブ）・Shift+F1/F2（ペイン）・F9/F10（タブ開閉）・Shift+F8/F9/F10（分割）・F12/Shift+F12（ズーム）があるのに F11 が無く、そのまま子プロセスへ流れていた。**窓そのものを動かすキーは、どちらの面にキーがあっても同じに効く**のが筋なので、F12 の隣に足した。F3・F5・F7 はわざと渡したまま（シェルの中の vim や htop に届くべきキー）| `gui/renderer.js ~ (?s)\A(?=.*e\.key === .F11.[^}]*cmdFullscreen\(\))` |
| 200 | 2026-09-14 | （同上の副産物）`drive.js` が写真のところで止まって走行が終わらない | **CDP の呼び出しに待ちの上限が無かった。** 返ってこない呼び出し1つで走行全体が止まる ── この日2回、`Page.captureScreenshot` が返らないまま18分立ち往生した（画面側の癖で、cian は生きていた）。`send` に上限（既定 30 秒）を付けて投げ、`shot:` は撮れなければ「撮れません」と言って**先へ進む**。数は `例外` とも `動かなかったキー` とも別に出す ── **写真は証拠であって検査ではない**。混ぜると「今日は画面が撮れないから全部赤」になる | `gui/drive.js ~ (?s)\A(?=.*send\(method, params = \{\}, ms = 30000\))(?=.*撮れません)(?=.*unshot)` |
| 201 | 2026-09-15 | 日本語を含むパスをシェルパネルに貼ると、カーソルが右にずれる（窓版・実機）| **桁と字数の取り違え。** エンジンは端末の桁で数えて（全角は2桁、続きのセルは飛ばす）カーソルの位置も桁で送るのに、窓は行の中を `text.length`（UTF-16 の単位）で歩いていた。ずれるのは**カーソルより前で run が切れているとき**だけで、色付きのプロンプト（PSReadLine）がそれを作っていた ── 素の行は1本の run なので、字数で歩いても同じ場所に落ちる。既にある `cellWidth`（端末版の `unicode-width` に当たるもの）で歩く。**検査は3回空振りした** ── 色が付かないと run が割れない／ESC を打ち込むと zsh の行編集が食う／`printf` だけだとプロンプトが次の行に描き直されてカーソルの行から全角が消える。`printf` にバックスラッシュを字として渡し、`read` で行を止めて測る。壊した版で落ちることを確認済み | `gui/drive.js ~ (?s)\A(?=.*全角の行でもカーソルの桁が合う)(?=.*read x)(?=.*cellWidth\(n\.textContent\))` |
| 202 | 2026-09-15 | `Ctrl+C` をコピーに割り当てたので、シェルで走っているものを止める手が右クリックしかない。`Ctrl+Q` を中断にしてほしい | `Ctrl+C` と同じ `\x03` を送る ── シェルから見れば同じ1バイト。帯（`Ctrl+Q 中断`）と `?` のキー一覧と右クリックの3か所に出した。端末の流れ制御の `Ctrl+Q`（XON）は、ここで止めるので届かない ── `Ctrl+S` を送って画面を止める道がそもそも無い（同時入力に使っている）ので、対になる XON も要らない。`keycover.py` が「一度も押されていないキー」と言ってきたので `drive.js` にも押す手順を足した | `gui/renderer.js ~ (?s)\A(?=.*if \(e\.key === 'q' && mod\(e\) && !e\.shiftKey\))(?=.*shellinput.*x03)(?=.*Ctrl\+Q)` |
| 203 | 2026-09-15 | 大規模な改修のあとのいつものチェック。**全角と半角の混在**でエラーや思わぬ動作が出ているので、テストの項目に入れてほしい | **窓版の幅の表が、エンジンと 11350 個の符号点で食い違っていた。** `gui/renderer.js` の `cellWidth` は East Asian Width の主な帯を手で並べたもので、エンジンと端末版が引く `unicode-width` とずれていた。目に見えたのは2つ ── 半角カナの濁点 `U+FF9E` を1桁と数えて（エンジンは0桁）`ｶﾞｷﾞｸﾞ` でシェルのカーソルが3字ぶん手前に落ちる、`✅`（`U+2705`）を1桁と数えて（エンジンは2桁）1桁右へ流れる。**どちらも全角と半角が混ざった行でしか出ない。** 表を `unicode-width` 0.2.2 から生成し直し（521 範囲・2.4KB）、全 1,114,112 符号点で一致することを確認した。`cellWidth` は `truncateMiddle`（パスの詰め方 ── 二つの前端で違っていた）と `:blame` の欄にも効くので、そこも一緒に揃う。**表は `cell_width_matches_unicode_width`（cian-core）が全符号点で確かめる** ── 表をずらす／縮める の2通りで変異テスト済み。検査の穴も塞いだ: 既にあった `★ 全角の行でもカーソルの桁が合う` は `.cur` の手前を `cellWidth` で数え直しているので、**`cellWidth` 自身が間違っていても必ず ok になる**（実際、5通り試して5通りとも通った）。`printf` の直後はカーソルが打った字の真後ろにいる、という別の見方で測る項目を足した ── 半角カナ・その濁点・絵文字・全角・半角英数を**同じ1行に**混ぜてある（1種類だけの行は壊れた版でも通る）。古い表で落ちることを確認済み | `gui/drive.js ~ (?s)\A(?=.*全角と半角が混ざってもカーソルは打った字の後ろ)(?=.*ｱｲｳ ｶﾞｷﾞ ✅ あaＡ1)` |
| 204 | 2026-09-16 | `y_and_ctrl_v_both_paste` が、この機械のクリップボードに載っているもの次第で落ちる。直してほしい | **テストが本物のシステムクリップボードを読んでいた。** 「何も入っていなければ `clipboard has no files` と言う」で `y` と `Ctrl+V` の道筋を見ていたので、**クリップボードにファイルが載っていると落ちる** ── `drive.js` を一周させると コピーが走って実際に載るため、`cargo test` → `drive.js` → `cargo test` の順で踏むと「さっきまで緑だったテストが落ちた」に見える。見たいのは**道筋であって中身ではない**（中身の規則は `cian-core` の `clip::plan` が、OS 側を差し替えられる形で持っている）ので、自前の register を埋めて `AlreadyHere` の言葉で見る ── `plan` は `own` が空のときだけ `os()` を呼ぶので、**OS のクリップボードはもう引かれない**。行き先は `TempDir` ではなく**ペインの `cwd`** から組む（mac の `/var` と `/private/var` は別の文字列で、親が一致せず `Go` に落ちた）。`y` の道筋を切る変異で落ちることを確認済み | `crates/cian-tui/src/tests.rs ~ (?s)(?=.*fn y_and_ctrl_v_both_paste)(?=.*already in this directory)(?=.*active_pane\(\)\.unwrap\(\)\.cwd)` |
| 205 | 2026-09-16 | `drive.js` の一周が `衝突の件数と ↑ 印の行数が一致` でたまに落ちる。直してほしい | **`z` の入力欄が開く前に、パスを打ち始めていた。** `goToPath` → `askFor` は非同期に開くのに、`['z'], ['type:…']` は四か所とも待ちが無かった（`Shift+F` も `Ctrl+f` も待っているのに、ここだけ）。開く前に打つと字が**窓への打鍵**になり、砂場のパスは mac では `/var/folders/…/T/cian-drive-XXXXXX` なので **`/T/` の `T` がトグルを開く**。あとの字がメニューの中を歩き（`j` が下、`k` が上）、次の `Enter` がその行を実行する ── 当たったのが「言語」の行で、表示が英語になり、**40 手あとで日本語の正規表現が当たらない**という顔で出ていた。衝突の数え方はどこも壊れていない。**砂場の名前は毎回違うので当たる行も毎回違い**、それが「たまに落ちる」の正体。四か所に待ちを入れ、あわせて**打ち終わった時点でメニューが開いていないか**を見て `例外` に数える（`bad` は普段 44 件あるので、そこへ混ぜると見えない）。`T` を開いてから打つ変異で鳴ることを確認済み | `gui/drive.js ~ (?s)(?=.*打った字がメニューに入った)(?=.*crashes\.push)(?=.*\['z', 'パスで移動'\], \['wait:600', ''\])` |
| 206 | 2026-09-16 | 別のセッションがソースを読んで見つけた報告の確認 ── 端末版に、全角・半角の混在で桁がずれる所が4つある | **4つとも本物で、数えたら 6 つあった。** `format!("{:<w$}", …)` は `{:<14}` と同じで**字**で詰めるので、`truncate()` が桁で切ったものを字で詰め直す ── 切るのと詰めるのとで**単位が違う**。報告の4つ（パレット `draw_palette`・`git log`・書庫 `draw_archive`・`:du`）に加え、`markdown.rs` のコード囲いの本文と見出しも同じ形だった（**報告は `render.rs` しか読んでいなかった**）。`git log` は二重で、`chars().take(author_w)` が字で切り（14字の日本語＝28桁）、そのうえ `{:<aw$}` が字で詰め直していた。直しは既にある `fit()`（＝`pad_to(&truncate(s,w),w)`）と `pad_to(&truncate_middle(…), w)` に寄せるだけ ── **同じ組は 4586 行目と 6560 行目で既に使われていた**ので、この6つは書き漏らしだった。報告の桁数のうち「`山田太郎です` に `{:<14}` で 22桁」は **20桁**が正しい（6字＝12桁＋空白8）。`{:<w$}` が素通りしていたのは `scripts/widths.py` が `\{:[<^]\d+\}` という**数字の形しか見ていなかった**から ── 幅を変数で渡す形と `chars().take(` を足し、安全な2行は理由つきで免除した。変異は両方向で確認 | `scripts/widths.py ~ (?s)(?=.*\[A-Za-z_\]\[A-Za-z0-9_\]\*\\\$\\\})(?=.*chars\\\(\\\)\\\.take\\\()` |
| 207 | 2026-09-19 | 社内に持ち込んでいる資材を、社内でビルド（梱包）できないか。crmaine は `crmaine-src` 一式を持ち込んで `gui/pack.js` している | **できた ── 足りなかったのは中身ではなく形だった。** `pack.js` が要るのは `gui/vendor/` が埋まっていることだけで、`npm install` も Rust も要らない。それは `cian-gui-win-x64.zip` に既に入っていたが、**あの zip は平らに展開される**ので、`gui/` の親から `Cargo.toml`（版）・README・LICENSE・`examples/`・`packaging/windows/` を読む `pack.js` からは拾えなかった。リポジトリと同じ形に並べた `cian-src-win.zip`（約30MB）を資材に足し、手順を `packaging/windows/SRC.ja.txt` に書いた。**持ち込みは 30MB ＋ エンジン10MB になる**（従来は 124MB）。出口で `pack.js`・同梱書体・Monaco・`SRC.ja.txt` の有無を検証する（`vendor.js` は写せなくても続けるので、書体の抜けた一式が黙って出た前科がある）。**手元で再現して確かめた** ── 同じ手順で並べた一式に偽の Electron と落としたエンジンを渡し、`cian.exe` と `cian-win-x64.zip` ができるところまで。**エンジンは社内では作れない**（Rust ＋ C）ので、そこは落としたものを渡す ── `--engine` は資材の名前のままでよく、中で改名する | `.github/workflows/release.yml ~ (?s)(?=.*name: Stage the source bundle)(?=.*cian-src-win\.zip)(?=.*SRC\.ja\.txt)` |
| 208 | 2026-09-20 | いったん cian の開発はやり切ったと思う。今残っているゴミファイル・不要ファイルの整理をして欲しいことと、GitHub にアップされている過去の資材の削除、不要ファイルの削除をお願いしたい。さらに、GitHub の Asset に今後アップする資材を君と壁打ちしたいぞ。会社でビルドすることを考えると、もう少し絞れるような気がするんだ | **資材を13点から3点＋`SHA256SUMS` に絞った。** 物差しは**「その機械で作れないものだけ置く」** ── 社内に持ち込む道が「`cian-src-win.zip` ＋ エンジン → `node gui\pack.js`」に決まった時点で、残りはその道で作れるものか、誰も取らないものになった。残したのは `cian-src-win.zip`（10MB）・`cian-server-win-x64.exe.zip`（3MB）・`cian-tui-win-x64.exe.zip`（5MB）の3点、合計18MB（従来 251MB）。**外したもの**: `cian-win-x64.zip`（123MB。社内で `pack.js` が同じものを作る。社内に Electron はある、と本人）、`cian-gui-win-x64.zip`（`cian-src-win.zip` が同じ中身を repo の形で持つ）、裸の `.exe` 2つ（**zip が通る網は裸も通るが、逆は通らない**）、Mac の5点（本人の Mac は Rust も Node も揃っている）。**ジョブは何も削っていない** ── 成果物は今まで通り全部上がり、publish の `Keep only what a release hands out` に載っているものだけがリリースに付く（`gh run download` で Mac のものは取れる）。落ちたものは行に出て、3点が揃わなければリリースを止める。**過去の資材も消した**: 資材ゼロの5リリース（v3.3.0/3.3.1/3.3.3/3.3.4/3.3.6）はリリースごと削除（タグは残した）、v3.3.5 以前の資材を全削除（約490MB）、Actions の成果物79本を削除。**v3.3.7 と v3.3.8 は残した** ── 同僚が 3.3.7 を28回落としていて、戻り先がそれしか無い | `.github/workflows/release.yml ~ (?s)(?=.*name: Keep only what a release hands out)(?=.*keep="cian-src-win\.zip cian-server-win-x64\.exe\.zip cian-tui-win-x64\.exe\.zip)` |
| 209 | 2026-09-20 | 診断は `:log` に集約しよう。「ログをとってくれ」が一番自然で一番言うと思うから。コミットログをみるのは `:gitlog` でどうだろう。svn コミットなら `:svnlog` だ。`:log` の提案は君の今の状態を出すで OK だ | **`:log` が診断の1語になった。** 単体で叩くと、この版と commit・画像の方式・キーボードのモード・IME・端末・設定をどこから読んでいるか・ログの行き先を1枚で出し、**送れるように `cian-state.txt` にも書く**（両前端とも。長い報告は下から切れるので、保存先の行は先頭の次に置いた ── 実機の pty で切れているのを見てから直した）。`:log on` / `:log off` / `:log <パス>` で**再起動なしに**診断ログを開始・停止できる（それまでは起動時の `CIAN_LOG` だけで、「もう一度起こして」と言えない種類の不具合のための道具なのに、もう一度起こさないと取れなかった）。**空いた名前の行き先**: コミットログは `:gitlog` / `:svnlog`、ペインの移動履歴は `:history`（`:back` の別名。**それまで端末版では `:history` がコミットログ、窓版では移動履歴**という食い違いがあった ── `parity.py` は画面の言葉を見るので、verb の意味の食い違いは映らない）。窓版の `:log2`（`:sessionlog` の別名）は本人も忘れていたので消した。engine 側の op は `diag`（`log` op はコミットログのまま ── 層ごとに別のものを log と呼ぶのが、この変更でやめたかったこと） | `crates/cian-tui/src/commands.rs ~ (?s)(?=.*"log" => self\.cmd_log\(rest\))(?=.*"gitlog")(?=.*"svnlog")(?=.*"history" => )` |
| 210 | 2026-09-20 | `:on`/`off`/`true`/`false`/`1`/`0` は短すぎるし意味がわからないから廃止したいな | **`:readonly` の引数は `on` と `off` だけになった。** `true`/`1`/`false`/`0` の4綴りは読む側の負担でしかなく、誰も打たない。**引数なしの `:readonly` はトグル**にした（隣の `:hidden` と同じ）── 反転するのは**先頭の対象の状態**で、それを選択全部に適用する。混ざった選択が片側に揃うのは、画面が自分と食い違わない唯一の答えだから。確実にしたいときのために `on` / `off` は残っている。なお `:on` などは**元から独立したコマンドではなく**、`:readonly` の引数だった（`:on` 単体は `unknown command`）── 僕が文字列リテラルを機械的に数えて誤報した | `crates/cian-tui/src/commands.rs !~ "true"` |
| 211 | 2026-09-20 | git と svn は6語に統一で良い。確認ダイアログで差を出すで良い | **操作の名前は片方だけになった。** `:stage` `:unstage` `:discard` `:commit` の4語が両方のリポジトリで効く（`:svnadd` `:svnrevert` `:svncommit` などは通るが一覧には出さない）。**`:commit` は今まで git 専用で、svn では `:svncommit` を打つしかなかった** ── ディレクトリはどちらか知っていて、バッジも状態行もそう言っているのに、打つ語だけが別だった。見るほうの2つは名前に VCS が入る（`:gitlog` / `:svnlog`、`:gitdiff` / `:svndiff`、依頼209の続き）。`:update` と `:resolve` は svn 専用のまま名前から `svn` を落とし、**git のディレクトリでは理由を言って断る**（`pull` は merge を伴うので入れていない、と ── 「svn 専用です」だけでは忘れられているのか断られているのか分からない）。**取り返しのつかなさの差は訊く画面が言う**: git の枠は「手元のリポジトリに記録します」、svn の入力欄は「コミットするとサーバに届きます」。git のコミットは手元で終わるが、svn のコミットは成功した瞬間に他の人が見ている。**svn の実機検査はこの機械では走らない**（`svnadmin` が無い）ので、`.svn` を1つ掘るだけの検査を別に足した ── `is_working_copy` はディレクトリの有無しか見ないので、バイナリ無しで分岐だけ試せる。変異テスト済み | `crates/cian-tui/src/commands.rs ~ (?s)(?=.*"commit")(?=.*self\.commit_prompt\(\))(?=.*"update")` |
| 212 | 2026-09-20 | AI は実務でほぼ使わない。たまにコマンドを聞くくらいだ。`:aicommit` は `:ai` に対応させたいな | **入口を6つから2つに。** `:ai` が引数で振り分け（`:ai commit` `:ai error` `:ai diff` `:ai log`、それ以外は会話への質問）、`:aicmd` だけ別に残る ── あれはシェル向けで、返るのが会話ではなく一行のコマンドだから。古い名前（`:aicommit` `:aierror` `:aidiff` `:ailog` とその別名）は通るが一覧には出さない。**`:ai <質問>` は会話欄に打ち込むだけで送らない** ── 何がこの機械から出ていくかを最後に見るのは本人（F3 の要約を鍵の後ろに置いてあるのと同じ理由）。**そして `cian.ai{}` が無ければコマンドパレットに出さない** ── 書いていない機械で、いちばんよく開く窓のいちばん上に、AI が6つ並んでいた。打てば今までどおり「未設定です」と答える。パレットの説明が「ローカルモデルとチャット」だったのも直した（既定は社内の endpoint で、ローカルは `api_base_url` を Ollama 等に向けたときの話）。**マニュアルからは消していない** ── あれは引くもので、何が要るかを書いてある方が親切だから（`:ai` の行に「init.lua の cian.ai が要ります」と出る） | `crates/cian-tui/src/palette.rs ~ (?s)(?=.*let has_ai = self\.ai\.is_some\(\))(?=.*starts_with\("ai"\))(?=.*\("aicmd")` |
| 213 | 2026-09-20 | `:touch` としたカーソルのファイルの日付を更新。`:touch hoge.txt` としたら hoge.txt を新規作成でどうだ？（日時指定は要らない、現在時刻だけでいい） | **`touch(1)` の二つの半分を、名前の有無で分けた。** 名前なしは選んでいるもの（マーク、無ければカーソル）の日時をいまに。名前ありはそのファイルを作る（従来の動き）。**ディレクトリにも打てる** ── `cian-core` に `touch_now` を足して `filetime` で書く。開いたハンドル越しに時刻を動かす従来の `ops::touch` ではディレクトリを掴めないし、消えていたら作ってしまう。日時の指定は入れない（本人）。窓版も同じで、engine に `touchnow` を足した | `crates/cian-core/src/ops.rs ~ pub fn touch_now` |
| 214 | 2026-09-20 | `:%!cmd` はそろそろ実装に向けて壁打ちしよう（4点とも君の提案で良い。ファイルの文字コードに合わせるってことだな） | **vi の filter が両前端に入った。** `:%!cmd` がファイル全体、`:.!cmd` がカーソル行、`:'<,'>!cmd` が選択範囲。**エディタパネルの中だけ**（一覧側の一括変換は取り消しの設計が別物になるので入れない）。シェルは下の段と同じもの（`cian.set_option("shell")`、無ければ `default_shell`）で、`cian-core::proc::shell_filter` が `-c` / `-Command` / `/C` を相手によって選ぶ。**文字コードはファイルのまま渡してそのまま読み戻す** ── Windows の `sort` や `findstr` はその code page を期待するので、入口で UTF-8 に直すと化けたものを並べ替えることになる（UTF-16 だけは例外で UTF-8。標準入力で読めるものがほとんど無い）。**失敗したらバッファを変えない** ── 終了コードが 0 でなければコマンドの言い分を状態行に出して中身はそのまま。失敗した stdout（空）を書き込むのは、filter が仕事を失う唯一の形で、しかも成功と見分けがつかない。取り消しは一手。**実機で3つの傷が出た**: 窓版で `sort` が `sortsort` になった（monaco-vim は同じものを `args` と `argString` の両方に入れる）、`:.!` がファイル全体に効いた（範囲は `line1`/`line2` ではなく `line`/`lineEnd`）、`%` に末尾の空行が混ざって先頭に空行が生えた（Monaco は末尾の改行を1行として持つ）。ついでに**同じ書き方で壊れていた `:combine 3` を直した** ── ずっと2行しか連結していなかった | `crates/cian-tui/src/viewer.rs ~ (?s)(?=.*fn split_filter)(?=.*fn filter_through_shell)` |
| 215 | 2026-09-20 | `:` の行の改善（カーソル・履歴・補完）をやりたい | **`:` の行にカーソル・履歴・Tab が入った。** それまで端末版は文字と Backspace と Ctrl+U だけで、**カーソルが存在しなかった** ── 長いパスの途中を打ち間違えたら、末尾から全部消すしかなかった。窓版の `:` は `<input>` なので OS の編集が最初から効いていて、その差は `parity.py` に映らない（画面の言葉は同じだから）。入ったもの: `←` `→` `Home` `End`（`Ctrl+A` / `Ctrl+E` も）、`Delete`、キャレットの描画、`↑` `↓` で**この起動で打った行**（失敗した行も。走らせる前に覚えるので）、`Tab` で**候補が一致するところまで**補完（行頭ならコマンド名、それ以外ならパス。複数なら名前を並べるだけで、勝手には選ばない）。**補完は `cian-core::path_completions` 1つを両前端が使う** ── 窓は engine の `complete` 越しに同じ関数を歩く。窓版は別名も候補に入れる（重複検出は窓で `dup`・端末で `duplicate` が本名で、名前だけ見ると同じ `dupli` が前端によって当たったり外れたりする）。**履歴はこの起動のあいだだけ**で、ディスクには残さない。書き込み口は `set_command_line` 1つに集約した ── キャレットを更新し忘れると、次の1文字が行の先頭に入る | `crates/cian-tui/src/keys.rs ~ (?s)(?=.*KeyCode::Tab => self\.complete_command)(?=.*walk_command_history)(?=.*KeyCode::Home)` |
| 216 | 2026-09-20 | あふのマスク（ディレクトリを移っても効く表示フィルタ）が無い。全部やりたいぞ | **`:mask *.log` が入った。** `/` の絞り込みは `go_to` で消える（それが `/` の仕事）。マスクは外すまで付いて回る ── 「今日はログを見ている」を画面に固定するもので、あふから引き継ぐ概念のうち最後まで残っていたもの。**`init.lua` の `mask` は「表示するだけで効いていなかった」という理由で削除されていた**（`cian-lua` にその旨のエラーが残っている）── これはその約束のほう。書き方は `*.log`、複数並べれば「どれか」（`*.log *.trc`。名前が2つの拡張子で終わることはない）、`/正規表現/` も同じ綴り。**ディレクトリはマスクに関係なく出る** ── 通り道を隠すマスクは、動くために外さなければならないマスク。読めないパターンは黙って0件にせず断る（全部隠れたマスクは空のディレクトリと見分けがつかない）。**効いている間は状態行に出す**（`マスク *.log (3/12 件)`。`..` を数に入れないよう直した ── 最初の版は「4/4 件」と隠していないふりをしていた）。glob は `cian-core` で既存の検索パターンに直すので、`/…/` が他の入口と同じ意味になる | `crates/cian-core/src/lib.rs ~ (?s)(?=.*pub fn mask_matcher)(?=.*pub fn set_mask)(?=.*pub mask: String)` |
| 217 | 2026-09-20 | 二画面の同期移動。君の鏡 `:mirror` を採用だ。行き先がない時は言って止まる方がいいね | **`:mirror` が入った。** 片方が動くと、もう片方が**同じ動き**をする ── 同じ場所ではなく同じ歩幅。`2026/` に入れば向こうも自分の `2026/` へ、上がれば同じだけ上がる。**行き先が無いときは言って止まる**（本人の指定）── 黙って追随をやめるミラーは、壊れたミラーと見分けがつかない。関係のない場所への移動（`:cd /var/log`）は「同じ動き」が定義できないので、そう言って動かさない。**規則は `cian-core::mirror_step` 1つ**で、端末版は打鍵ごと、engine は窓からの呼び出しごとに同じ関数を通る。**入り口は1つ**: 端末版は `handle_key` / `handle_mouse` の外側、engine は `handle` の外側で前後の cwd を比べる ── `jump_to` の呼び出し元は10か所以上あり、1か所ずつ追随を書けば次に足した場所で忘れる。窓版は engine が動かしたもう片方を同じ返事（`mirrored`）で受け取って描き直す。効いている間は状態行に出す | `crates/cian-core/src/ops.rs ~ (?s)(?=.*pub fn mirror_step)(?=.*MirrorStep::Missing)` |
| 218 | 2026-09-20 | 別名の整理。`:select`・`:source` は窓版から別名を外して、`:attr` `:duplicate` `:notepad` `:unzip` `:help`（`:man` は別名で残す）`:markall` `:new` `:rename` `:resolve` を正にする | **同じ機能を、両前端が同じ名前で呼ぶようにした。** 機械で洗って13件の食い違いが出た。**危なかったのは2つ** ── `:select` は端末版でマーク・窓版でビジュアル選択、`:source` は端末版で init.lua 再読込・窓版で Markdown 表示。**設定を直して `:source` と打つと窓版ではプレビューが開く**という形で、依頼209の `:history` と同じ事故。窓版から別名を外したら `parity.py` が「端末版の `:select` / `:source` が窓版の辞書にない」と鳴ったので、**消すのではなく端末版の意味で窓版に足した** ── 消すだけでは、端末版で打てる語が窓版で死ぬ。残り9件は「同じ機能・違う本名」で、本人の指定どおり端末版側に揃えた（打てる綴りは1つも減らしていない。変えたのは一覧に出る名前と、機械が本名と見なす順だけ）。窓版で同じ機能に2つ目の項目が立っていたもの（`:man` `:stat` `:step`）は別名に畳んだ ── 一覧に二度出るうえ、どちらが本名か機械に分からない。**`scripts/names.py` を足した**: ①本名の食い違い（天井0）／②片方にしか無い名前／③別名の落差。`parity.py` は画面に出る言葉を見るので、打つ名前の食い違いはこれまでどの物差しにも映っていなかった | `scripts/names.py ~ (?s)(?=.*① 本名の食い違い)(?=.*def tui_commands)(?=.*def gui_commands)` |
| 219 | 2026-09-20 | 設定画面を見て、直す範囲を決めてくれ | **見たら、僕の評価のほうが間違っていた。** 「設定が init.lua と二重管理で負債」と書いたが、画面は既に**書き込み先のファイル名を出し**、各項目に `init.lua` の鍵の名前と「窓版だけ／端末版だけ」を添え、`init.lua` が Lua として読めないときは**保存を止めて Lua の行番号つきの理由を出す**。書き込みは `settings_edit.rs` が**外科的に**やる ── 丸ごと書き直さず、触る行だけ差し替え、消すときは行を削らずコメントに戻し、改行コードも元のまま、`.bak` を残して一時ファイル経由で置換。crmaine が同じ所で転けた経緯が註に全部書いてある。**作り直す理由が無い。** 直したのは1か所だけ: **保存が断られたとき、シートの中に残らなかった** ── 書けないディレクトリに保存すると理由は状態行に出るが、シートの下は「1 件の変更を init.lua に書きます」と未来形のままだった。状態行は次の知らせで流れるし、設定の保存は「押したはずなのに効いていない」がいちばん起きる場所だ。断られた理由をシートの下に色付きで残し、変更は消さずに置く（直してもう一度押せば書ける）。実機で確認済み | `gui/renderer.js ~ (?s)(?=.*settings\.failed = status\.msg)(?=.*settings\.failed = null)(?=.*保存できませんでした)` |

## 増やすとき

依頼を受けて何かを入れたら、**その場でここに1行足す**。あとでまとめてやると、
まとめる前に自分で消します（それが起きたのがこの表の1行目です）。

検査は「その依頼が満たされていること」を見るもので、実装の形を凍結するもの
ではありません。**作り直して構いません。ただし検査を通したまま**、あるいは
検査ごと変えるなら**先に訊いてから**。
